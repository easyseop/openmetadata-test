# OM_TEMP 1.13.1 → 1.13.2 검사기 간단 학습 가이드

**전체 순서:** 6/11 · [전체 목차](./OM_TEMP_1.13.1_1.13.2_예행연습_전체목차.html)

**문서 이동:** [← 이전 — 코드 정리 및 등록 준비](./OM_TEMP_1.13.1_코드정리_및_등록준비_가이드.html)

## 공통 경로 설정

이 페이지의 명령을 실행할 터미널에서 검사기 저장소로 이동한 뒤 공통 경로를 불러옵니다. 다른 컴퓨터에서는 clone 위치만 바꾸면 됩니다.

```bash
cd <검사기-저장소-clone-경로>
```

```bash
source harness/rehearsal_env.sh
```

`OM_TEST_REPO`는 검사기 저장소, `OM_CODE_REPO`는 OpenMetadata 코드 작업 폴더, `KB_SOURCE_REPO`는 실제 커스터마이징 원본 폴더를 가리킵니다. 새 터미널을 열면 다시 실행합니다.

## 1. 문서 범위

이 문서는 `openmetadata-test` 검사기 카탈로그 23종과, 이번 예행연습에서 추가한 **공용 파일의 BANK-OM ID별 코드 정의 검사** 1종을 설명합니다. 실행 명령을 따라 하는 문서가 아니라, 각 검사가 브랜치 전략의 어느 시점에서 무엇을 확인하는지 먼저 익히는 문서입니다.

`T42`와 같은 번호는 OpenMetadata 기본 기능이 아니라 `openmetadata-test` 검사기 저장소에서 관리하는 내부 검사 ID입니다.

## 2. 판정 결과

| 표시 | 의미 | 다음 조치 |
|---|---|---|
| `PASS` (`pass`) | 자동 검사 조건을 모두 충족했습니다. | 다음 단계를 계속합니다. |
| `APPROVAL` (`approval`) | 자동으로 오류라고 확정할 수는 없지만 담당자 검토가 필요합니다. | 이유를 확인하고 승인 근거를 남긴 뒤 계속합니다. |
| `BLOCK` (`block`) | 위반 또는 기능 누락을 확인했습니다. | 코드나 등록자료를 고친 뒤 같은 검사를 다시 실행합니다. |
| `ANALYSIS_ERROR` (`analysis_error`) | 입력 누락이나 환경 오류로 검사기가 안전하게 판단하지 못했습니다. | 이유에 적힌 입력·환경 문제를 해결하고 다시 실행합니다. |

## 3. 브랜치 전략에서의 사용 위치

```text
[새 공식 OpenMetadata branch] -- A. 병합 전 영향 확인 --\
                                                        +--> B. 다음 커스텀 브랜치에서 vendor merge
[직전 커스텀 브랜치] -------------------------------/                  |
                                                           Git 충돌이면 사람이 해결
                                                                         |
                                                     C. 등록자료·소스 규칙 검사
                                                                         |
                                                     D. 소스·동작 test 검사
                                                                         |
                                                     E. 결과 집계·승인
                                                                         |
                                                     [검증 표시 tag → 릴리즈 브랜치]
```

> **Git 충돌은 검사기 판정이 아닙니다.** vendor merge 중 Git이 같은 코드 구간의 두 변경을 자동으로 합치지 못하면 충돌로 중단합니다. 담당자가 코드를 선택·수정하고 merge를 완료한 뒤에 검사기를 실행합니다.

아래 표는 **실제 상황 예시 → 검사기가 묻는 질문 → 결과를 받은 뒤의 조치** 순서로 읽습니다.

### A. 병합 전 영향 확인

| 검사기 | 실제 상황 예시 | 검사기가 묻는 질문 | 결과와 조치 |
|---|---|---|---|
| T93 정책 노후화 | 새 공식 버전에 `openmetadata-new-module/` 폴더가 생겼지만 경로 규칙에는 이 폴더가 없습니다. | 기존 민감·감시 경로 규칙이 새 버전의 실제 파일을 계속 찾고, 새 모듈도 분류했는가? | 0개 파일만 찾는 규칙은 `APPROVAL`, 미분류 새 모듈은 `ANALYSIS_ERROR`입니다. 경로 규칙을 보완한 뒤 T93을 다시 실행합니다. |
| T42 공식 변경 영향 | BANK-OM-001이 `CollectionDAO.java`를 감시하는데 공식 1.13.2에서 이 파일이 변경됐습니다. | 이전·새 공식 버전의 diff가 Manifest `upgrade_watch`와 겹치는가? | 겹치면 `APPROVAL`입니다. 담당자가 변경 diff를 검토합니다. Git 충돌이나 기능 오류가 확정된 것은 아닙니다. |
| T51·T52 구조화 diff | 공식 JSON Schema의 `type` 값이 `string`에서 `array`로 바뀌었습니다. | API·JSON·Helm·DB·의존성의 키·타입·값·목록이 어떻게 바뀌었는가? | 변경 전·후 구조를 결과로 남깁니다. 이 변화가 BANK-OM을 깨는지는 담당자와 test가 판단합니다. |
| T80 LLM 영향 메모 | T42에서 인증 경로가 걸렸고, 릴리즈 노트에도 인증 변경이 있습니다. | 담당자가 먼저 볼 의미 변경 후보는 무엇인가? | 근거를 포함한 검토 메모만 만듭니다. T80은 `PASS`·`BLOCK`을 판정하지 않습니다. |

### B. vendor merge와 브랜치 관계 확인

| 검사기 | 실제 상황 예시 | 검사기가 묻는 질문 | 결과와 조치 |
|---|---|---|---|
| T24~T29 통합·계보·생존 | 공식 1.13.2 branch를 병합했다고 했지만 새 커스텀 branch의 Git 이력에 승인한 공식 1.13.2 commit이 없는 경우입니다. | 새 커스텀 branch가 승인한 공식 코드 이력을 포함하고, 활성 BANK-OM 변경도 남아 있는가? | 둘 중 하나라도 없으면 업그레이드 후보로 사용하지 않습니다. 병합 기준과 커스텀 코드 누락을 확인합니다. |
| T20·T21 패치 재적용 | 선택 `patch-replay` 방식에서 BANK-OM commit을 재적용하다가 충돌이 발생했습니다. | 고정한 commit을 순서대로 재적용했는데 충돌·빈 변경·소스 누락이 있는가? | T20은 충돌을 보고하고 중단합니다. T21 해결 모드에서는 담당자가 해결합니다. 현재 기본 vendor merge에서는 필수가 아닙니다. |
| T22 clean-room replay | 같은 patch-lock으로 다시 만든 코드와 검사 대상 코드의 Git tree가 다릅니다. | 고정 공식 버전과 patch-lock만으로 정확히 같은 코드를 재현할 수 있는가? | tree가 다르면 누락되거나 등록되지 않은 변경을 찾습니다. 현재 기본 vendor merge에서는 필수가 아닙니다. |
| T24·T11 검사 대상/patch lock | commit `AAA` 기준으로 test한 뒤 커스텀 branch에 commit `BBB`가 추가됐습니다. | 검사에 사용한 commit·파일 식별값이 검사 중에 바뀌지 않았는가? | 기준 commit이 바뀌면 이전 test 결과를 사용하지 않고 새 검사 대상 lock으로 다시 검사합니다. |

### C. 등록자료·소스 규칙 검사

| 검사기 | 실제 상황 예시 | 검사기가 묻는 질문 | 결과와 조치 |
|---|---|---|---|
| T30 commit 규칙 | commit 하나의 본문에 `BANK-OM-001`과 `BANK-OM-002`가 함께 있습니다. | 하나의 커스터마이징 commit이 하나의 BANK-OM ID만 나타내는가? | 두 기능의 변경을 별도 commit으로 분리하고 ID를 각각 적습니다. |
| T31 ID·series 규칙 | BANK-OM-002가 `1/2`, `2/2` 두 commit으로 등록됐지만 후보 브랜치에는 `1/2`만 있습니다. | 하나의 BANK-OM ID를 이루는 모든 commit이 정해진 순서와 개수대로 있는가? | 누락된 commit을 복원하거나 Manifest `series`를 실제 설계에 맞게 수정한 뒤 재검사합니다. |
| T32 최종 상태 | BANK-OM-001이 `INSTANCE_CODE`를 추가했지만 뒤 commit이 그 코드를 삭제했습니다. | 등록된 BANK-OM 변경이 최종 커스텀 코드에서도 실제 변화를 남기는가? | 의도한 삭제면 retirement 근거를 등록하고, 실수면 삭제 commit을 보완합니다. |
| T40 구현 범위 drift | Manifest에는 `Entity.java`만 적혀 있지만 실제 Git diff에는 `CollectionDAO.java`도 있습니다. | Manifest가 허용·필수로 선언한 파일과 실제 변경 파일이 일치하는가? | 누락된 파일의 BANK-OM 소유 관계를 확인한 뒤 Manifest나 코드 범위를 수정합니다. |
| T41 민감 경로·의도 | 인증 코드를 바꾸었지만 변경 의도와 승인 근거가 없습니다. | 민감 경로를 건드린 변경이 선언된 범위 안에 있고 승인 근거를 갖추었는가? | 자동 통과시키지 않습니다. 변경 의도·영향·승인 근거를 확인하고 필요하면 코드 범위를 줄입니다. |
| T43 유지 부담 | 정책 상한이 “코어 수정 5개”인데 후보 코드가 8개를 바꾸었습니다. | 코어 수정 개수·변경량·반복 충돌률·집중 경로가 승인한 상한 안인가? | 상한을 넘으면 원인 경로를 보고합니다. 코어 수정을 줄이거나 예외 승인을 받은 뒤 재검사합니다. |
| T50 선언형 검사 | Manifest는 JSON의 `/enabled`가 `true`라고 선언했지만 후보 파일은 `false`입니다. | Manifest에 승인한 JSON/YAML 값·파일 hash·Python 모듈 파일이 후보 코드에 실제로 있는가? | 선언과 실제 값이 다르면 `BLOCK`, 파일을 읽지 못하면 `ANALYSIS_ERROR`입니다. |
| 필수 test 존재 | Contract에 `test_query_report_create`를 적었지만 test 파일에 그 함수가 없습니다. | Contract가 지정한 test 파일과 test 이름이 실제로 있는가? | 없는 test를 생성하거나 Contract의 잘못된 test 이름을 고친 뒤 재검사합니다. |
| T60 Contract 연결 | `CONTRACT-QUERY-REPORT`에 필수 test는 있지만 `BANK-OM-002`와의 연결이 없습니다. | 업무 동작 기준·필수 test·BANK-OM ID가 하나의 검증 관계로 연결됐는가? | `contracts.yaml`·Manifest·Registry의 ID 연결을 일치시킨 뒤 재검사합니다. |
| 공용 파일 ID별 코드 정의 | `Entity.java`를 BANK-OM-001·002가 함께 쓰는데 001의 `INSTANCE_CODE`만 있고 002의 `QUERY_REPORT`는 없습니다. | 공용 파일 안에 각 BANK-OM ID로 승인한 실제 코드 정의가 모두 있는가? | 없는 정의는 `BLOCK`입니다. 실제 코드를 복원하거나 승인한 정의가 틀렸는지 검토합니다. 주석에 적은 단어는 정의로 인정하지 않습니다. |

### D. 소스·동작 test 검사

| 검사기 | 실제 상황 예시 | 검사기가 묻는 질문 | 결과와 조치 |
|---|---|---|---|
| T61 patch-kill | BANK-OM-002 변경을 뺀 코드에서도 QueryReport 필수 test가 통과합니다. | 해당 BANK-OM 변경을 제거했을 때 필수 test가 정말 실패하는가? | 변경을 빼도 test가 통과하면 그 test는 해당 커스터마이징을 검증하지 못하므로 `BLOCK`입니다. 현재 소스 실험 2/5만 입증됐습니다. |
| T62 test·SHA 연결 | 업그레이드 후보 commit `AAA`의 test 결과를 다른 commit `BBB`에 붙이려고 합니다. | test가 실행된 정확한 업그레이드 후보 commit·이미지·검사 정의와 결과가 묶였는가? | 식별값이 다르면 이전 test 결과를 무효로 보고 `BBB`에서 다시 test합니다. 실행기는 구현됐지만 운영 실행 증거는 대기 중입니다. |
| T90 업그레이드 차등 test | 같은 API 요청에 1.13.1은 `200`, 1.13.2 후보는 `500`을 반환합니다. | 구·신 버전에 같은 데이터·요청을 넣었을 때 승인한 비교 항목의 결과가 같은가? | 승인하지 않은 차이는 실패 증거로 남기고 원인을 해결합니다. 결과 형식은 구현됐지만 실제 스택 실행은 대기 중입니다. |
| T63 UI typecheck 기준선 | 공식 코드는 `Table.tsx / TS2322` 오류가 없는데 후보 코드에 1개 생겼습니다. | 같은 Node·Yarn 환경에서 후보 코드의 새 TypeScript 오류 경로·코드가 공식 코드보다 늘었는가? | 새 오류는 `BLOCK`입니다. 새 오류가 없어도 공식 기준선이 0이 아니면 담당자 검토를 위해 `APPROVAL`입니다. |

### E. 결과 집계·승인·릴리즈

| 검사기 | 실제 상황 예시 | 검사기가 묻는 질문 | 결과와 조치 |
|---|---|---|---|
| T70 정책 자기 완화 방지 | PR이 “필수 test 3개” 규칙을 “1개”로 줄인 뒤, 줄어든 새 규칙으로 자기 자신을 통과시키려고 합니다. | 검사 규칙을 약화하는 변경을 변경 전 기준으로도 평가했는가? | 변경 전 기준을 적용해 자기 완화 우회를 막습니다. 정책 변경은 별도 승인을 거칩니다. |
| T91 검증·배포 대상 일치 | 검사는 이미지 digest `sha256:AAA`로 했지만 배포 대상은 `sha256:BBB`입니다. | 검사한 commit·이미지·설정과 실제 승격·배포할 대상의 식별값이 모두 같은가? | 하나라도 다르면 배포하지 않습니다. 현재 엔진은 구현됐지만 실제 승격 실행은 대기 중입니다. |
| T13 판정 집계 | 전체 결과가 `PASS`, `APPROVAL`, `BLOCK` 하나씩입니다. | 여러 검사 결과를 합칠 때 더 심각한 결과가 약화되지 않는가? | 최종 결과는 `BLOCK`입니다. `ANALYSIS_ERROR`도 통과로 바꾸지 않습니다. |

## 4. 1.13.1 → 1.13.2 예행연습에서의 사용 순서

1. 1.13.1 기준 Manifest·Registry·Contract와 공용 파일 ID별 코드 정의를 승인합니다.
2. OpenMetadata 1.13.2 포크 브랜치를 준비한 뒤 **A 검사**로 병합 전 영향을 확인합니다.
3. 직전 커스텀 브랜치와 1.13.2 포크 브랜치를 다음 커스텀 브랜치에서 vendor merge합니다.
4. Git 충돌이 있으면 담당자가 충돌 코드를 해결합니다.
5. merge가 완료된 다음 커스텀 브랜치에서 **B·C 검사**를 실행합니다.
6. 필요한 실행 환경을 준비한 뒤 **D 검사**를 실행합니다.
7. **E 검사**로 결과를 집계하고 필수 `APPROVAL`의 승인 근거를 확인합니다.
8. 모든 `BLOCK`과 `ANALYSIS_ERROR`가 해결되고 필수 승인이 완료된 동일한 1.13.2 업그레이드 후보 commit에만 검증 표시 tag를 붙이고 릴리즈 브랜치로 승격합니다.

## 5. 현재 경계

- 이 페이지는 검사기를 익히기 위한 개념 문서입니다. 실행 명령·입력 파일·예상 출력은 후속 예행연습 실행 페이지에서 단계별로 제공합니다.
- T61·T90·T91은 실제 runtime·승격 환경의 운영 증거가 아직 필요합니다.
- T62는 실행기가 구현됐지만 운영 run 증거가 아직 필요합니다.
- T80은 보조 자료만 만들며 통과·차단을 판정하지 않습니다.
- T20·T21·T22·patch-lock은 선택 `patch-replay` 방식용이며, 현재 기본 `vendor merge` 방식의 필수 절차로 표시하지 않습니다.

## 6. 다음 단계

앞 단계에서 준비한 ID별 commit 후보 branch, 공용 파일의 ID별 코드 정의와 Manifest·Registry·Contract를 `plan → 사용자 승인 → apply` 순서로 반영합니다. 다음 실행 가이드에서 등록·소스·공용 코드 검사의 입력과 결과를 직접 확인합니다.

**문서 이동:** [← 이전 — 코드 정리 및 등록 준비](./OM_TEMP_1.13.1_코드정리_및_등록준비_가이드.html) · [다음 — 등록 승인·apply 및 1.13.1 기준 검사 →](./OM_TEMP_1.13.1_등록승인_apply_및_기준검사_가이드.html)

# Claude 위키 검토 대응 결과

검토 파일: `WIKI_REVIEW_20260729.md`

검토 대상이 사용한 코드 기준: `dfc43f1`

현재 작업 기준: `codex/strict-manifest-gates`의 후속 작업본

검토 의견은 현재 코드·스키마·등록파일·렌더링 결과와 다시 대조했다. 이전
커밋에는 맞았지만 현재 구현에는 맞지 않는 지적은 그대로 되돌리지 않았고,
현재도 재현되는 문제만 수정했다.

| 항목 | 판단 | 현재 대응 |
|---|---|---|
| F1 Manifest 스키마 불일치 | 이전 작업본 기준 지적 | 현재 스키마는 v1·v2를 구분해 검증한다. OM_TEMP 1.13.0·1.13.1 등록본은 `schema_version: 2`와 `implementation.changed_paths`를 사용한다. |
| F2 `candidate_additional_paths` 잔존 | v1 호환 코드에는 의도적으로 잔존 | OM_TEMP v2에서는 사용하지 않는다. v1 등록 묶음을 읽기 위한 호환 경로로만 남겼으며 v2가 v1 필드를 섞으면 검증을 차단한다. |
| F3 독립 HTML 상대경로 | 수용 | 보고용 이미지 20개와 충돌 근거 5개를 `data:` URI로 내장했다. 빌드 단계에서 남은 로컬 의존성을 발견하면 실패하도록 했다. |
| F4 `NOT APPLICABLE` 판정 | 수용 | T25-R을 기계 판정처럼 표시하지 않고 전용 CLI가 없는 참고 상태로 분리했다. |
| F5 없는 문서 링크 | 수용 | 현행 위키의 Manifest 상세 페이지로 연결했다. |
| F6 보고용 그림 스타일 | 부분 수용 | 보고용 내용은 위키 컴포넌트로 재구성했다. 기존 도식은 정보 구조를 유지하되 글꼴·여백·가독성을 위키 안에서 확인한다. |
| F7 1.13.1 원격 부재 | 수용 | 1.13.1 진단 branch와 후보는 로컬 기록이며 원격 branch·tag가 아님을 결과와 배포 상세에 명시했다. |
| F8 미실행 T90의 `12 pass` | 수용 | 실측 결과가 아니라 가상 형식 예시로 표시하고 상태를 `APPROVAL` 색으로 분리했다. |
| F9 T25-R 명령 결함 | 수용 | 잘못된 diff 기호와 다른 검사를 가리키던 명령을 제거했다. 전용 CLI가 없으며 모듈 test로만 확인된다는 현재 상태를 표시했다. |
| F10 `kb-openmetadata` 실행기 혼용 | 수용 | 공용 실행기 진입점 4개를 `harness/`에 추가하고 OM_TEMP 1.13.1 등록 경로를 사용하도록 문서 명령을 고쳤다. |
| F11 Cycle의 T42 누락 | 수용 | patch 갱신 뒤, custom 병합 전에 `T42 사전 영향 확인·등록 갱신` 단계를 추가했다. |
| F12 완료·상신·보존 조건 | 수용 | 각 검사 상세에 실패 예시와 완료 조건을 표시한다. BLOCK·APPROVAL·ANALYSIS ERROR 조치와 승인 기록 항목을 추가했고, 조직 장기 보관 정책은 아직 미정이라고 명시했다. |
| F13 111 비교 축 | 수용 | `공식 1.13.0 ↔ custom 1.13.0 차이 111개`로 표시해 공식 업그레이드 834개와 구분했다. |
| F14 검사별 BLOCK 예시 없음 | 수용 | 각 검사 페이지에서 해당 검사의 실패 판정 예시와 재실행 행동을 자동 표시한다. |
| F15 상태 칩 항상 초록 | 수용 | 검사별 `statusLevel`을 사용해 PASS·APPROVAL·BLOCK·중립 상태의 색을 구분한다. |
| F16 숨은 중복 페이지 | 수용 | 메뉴에서 접근하지 않던 6개 과거 `<article>`을 삭제했다. |
| F17 순서·빈 페이지·대비·표 잘림 | 수용 | 목차를 1→2→3→4→5 순으로 정렬하고 T90·T91 본문을 연결했다. 흐름 글자 대비를 높이고 고정 폭 표를 제거했다. |

## 검증 결과

- 전체 Python test: `314 passed, 47 skipped`
- Manifest·업그레이드 관련 집중 test: 통과
- 공용 실행기 4개: `--help` 정상 종료
- 1280px 브라우저: 가로 넘침 없음
- T90 상태: 초록 PASS가 아닌 호박색 APPROVAL
- 브랜치 도식: 병합 전 T42 표시 및 도식 내부 글자 겹침 없음
- 독립 HTML 충돌 근거 링크: 5개 모두 내장 `data:` URI
- 브라우저 console error: 없음

`skipped`는 Java·Maven·Yarn 또는 별도 실행 환경이 필요한 test다. 이 수치를
행내 Runtime test나 운영 배포 완료로 해석하지 않는다.

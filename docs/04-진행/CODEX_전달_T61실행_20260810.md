# Codex 전달 프롬프트 · 2026-08-10 T61 실행

> 이 파일은 Codex 에게 그대로 붙여 넣는 프롬프트다.
> 아래 코드블록 전체를 복사해 쓰고, 맨 끝에 `## 할 일` 을 한 줄 붙인다.
> 사실관계 정본은 `docs/04-진행/세션전체기록_T61실행_20260810.md` 다.

```text
openmetadata-test 저장소, branch `codex/phase-bundling-safety-fix-20260808`.
2026-08-10 에 Runtime Contract 가 이 프로젝트에서 처음으로 실제 실행됐다.
아래를 파악한 뒤 지시를 기다린다.

## 먼저 읽을 것

1. docs/04-진행/세션전체기록_T61실행_20260810.md   ← 정본. 배경부터 다음 단계까지
2. docs/04-진행/실행결과_T61_순정대조_20260810.md   ← 결과 요약
3. evidence/om-1.13.1-runtime-t61-20260810/         ← 증거 원본
4. STATUS.md 최상단, docs/04-진행/CLAUDE_REVIEW_HANDOFF.md 최상단

커밋: `74df62e`(증거·핸드오프 갱신·2026-08-09 문서 8건),
`e78bc90`(전체 기록·재테스트 도구), 그리고 이 파일을 추가한 커밋.

## 실행 결과

커스텀 ghcr.io/easyseop/openmetadata-bank:1.13.1-bank-8ac18ad0
       digest sha256:a28ade3e…  candidate 8ac18ad0…
       → pass 9 / fail 0 / error 0 / skipped 0 · verdict pass

순정   docker.getcollate.io/openmetadata/server:1.13.1
       digest sha256:eaa31858…  revision afcb2d2c…
       → fail 8 / pass 5 / error 0 / skipped 0 · verdict block · exit 1

순정 revision `afcb2d2c…` 는 runtime-patch-kill-plan.yaml 이 BANK-OM-001 의
without_patch_sha 로 지정한 커밋과 같다. rehearsal compose 가 그 커밋에
고정돼 있어서다.

## T61 은 3/5 다. 5/5 로 승격하지 마라

source 2/5 (BANK-OM-006·007) + runtime 1/5 (BANK-OM-001) = 3/5.
BANK-OM-002·003 은 미완이며 이유는 셋이다.

1. 계획서는 per-ID 전임을 요구한다. 002 는 `a2566fac`(InstanceCode 는 있고
   QueryReport 만 없음), 003 은 `4108411c`. 우리는 패치를 전부 뺀 스택 하나를
   썼으므로 실패의 ID 귀속이 성립하지 않는다.
2. 계획서 candidate 는 `849ae756…`, 배포 이미지는 `8ac18ad0…` 로 다르다.
3. 계획 파일이 harness/registrations/kb-openmetadata/ 에만 있고,
   오늘 후보의 등록 폴더 harness/registrations/om-temp-1.13.1/ 에는 없다.

## 오독하기 쉬운 것 — 반드시 확인

- **test_sybase·test_tibero 가 순정에서 pass 한 것은 껍데기가 아니다.**
  두 test 는 OPENMETADATA_PRODUCT_REPO 의 소스 파일을 읽는 계약이라 서버
  이미지 교체로는 구조적으로 죽지 않는다. 이미 source patch-kill
  (`6e5b654f`·`41b224ad`)로 입증된 2/5 다. "순정에서 통과 = 껍데기" 규칙을
  기계적으로 적용하면 이 둘을 오판한다.
- **var/rehearsal/ 은 .gitignore 대상이다.** 증거를 남기려면 evidence/ 로
  복사해야 커밋된다.
- **MySQL 은 ./docker-volume/db-data 호스트 bind mount 라 compose project
  name 으로 분리되지 않는다.** ES 만 분리된다.

## 새로 확인된 결함 3건

[Serious] BANK-OM-005 생존 미입증.
  test_korean_ime::test_hangul_composition_roundtrip 이 순정 digest
  sha256:eaa31858… 에서 outcome: pass. 이 ID 의 필수 런타임 test 는 이것
  하나뿐이라 기능이 사라져도 검사기가 모른다. 같은 파일의
  test_hangul_composition_source_guard 는 구현돼 있으나 contracts.yaml 의
  required_tests 에 미등재.
  반례: evidence/om-1.13.1-runtime-t61-20260810/t61-vanilla-negative-control/test-run-set.yaml

[Serious] BANK-OM-003·004 의 API 계층 test 판별력 없음.
  test_failed_assertion_projection, test_extended_column_projection 이 순정에서
  pass. rendered_row 쪽만 죽어서 계약 전체로는 판별되지만 절반은 민감하지 않다.

[Blocking] 순정 롤백이 데이터 계층에서 불가능.
  BANK 데이터가 있는 DB 에 순정을 올리면 마이그레이션이 exit 1.
    java.lang.IllegalArgumentException: Sybase
      at CreateDatabaseService$DatabaseServiceType.fromValue(CreateDatabaseService.java:552)
      at SecretsManagerUpdateService.retrieveServices(SecretsManagerUpdateService.java:188)
  bank_mock_sybase·bank_mock_tibero 의 serviceType 을 순정 enum 이 모른다.
  **같은 이미지가 빈 DB 에서는 exit 0 으로 정상 기동한다.** 순정 결함이 아니라
  데이터 비가역성이다. 로드맵 14-1 의 "롤백 계획 없음" 을 "데이터 계층 롤백
  불가" 로 승격해야 한다. 이건 검사기로 풀리지 않는 설계·정책 사안이다.

## 저장소 변경 (기본 동작·CI 무영향)

- docker/rehearsal/docker-compose.override.yml
    contract-runner platform → ${OM_CONTRACT_RUNNER_PLATFORM:-linux/amd64}
- docker/rehearsal/test.sh
    OM_CONTRACT_RUNNER_PULL=0 이면 pull 생략, 로컬 image 없으면 exit 2 fail-closed
- docker/rehearsal/t61_vanilla.sh (신규)
    순정 negative control 전용. 마이그레이션 non-zero 시 즉시 중단.
    runner exit 0 이 오히려 문제라는 해석을 출력에 포함.
- CLAUDE.md 읽기 순서 0번에 세션전체기록 연결

## 실행 환경 (Apple Silicon 한정)

에뮬레이션된 x86 Chromium 이 OpenMetadata SPA 를 렌더링하지 못한다
(메모리 382MB 사용, CPU 1코어 100%, load 이벤트 120초 초과).
prepare_runtime_contract_environment.py:243 의 browser_state() 에 우회
수단이 없어 API 전용 계약 6개까지 함께 막힌다. arm64 네이티브 러너로
빌드하면 같은 화면이 1.7초에 렌더링된다. colima 메모리 증설로는 안 고쳐진다
— 자원이 아니라 아키텍처 문제다. 재현 절차는 세션전체기록 6부.

## 지금 막혀 있는 것

T61 나머지 2건과 1.13.2 Candidate 검증이 **같은 이유로** 막혀 있다:
OM 서버 이미지 빌드(전임 a2566fac·4108411c, 그리고 우리 1.13.2 커스텀).
8GB 로컬 기기는 OOM 이력이 있어 CI 빌드가 필요하다. 이것이 유일한 자원
병목이며, 나머지 결함은 자원을 늘려도 풀리지 않는다.

## 승격 금지

이 배치는 배포 준비 상태가 아니다. 1.13.2 Candidate 검증, BANK-OM-002·003
per-ID negative control, T90/T91/T94 는 여전히 미실행이다. 소스 검사 통과나
Contract 9/9 를 운영 배포 가능으로 승격하지 마라.
```

---

## 맨 끝에 붙일 `## 할 일` 예시

| 목적 | 붙일 문장 |
|---|---|
| CI 빌드 | 전임 `a2566fac`·`4108411c` 와 1.13.2 커스텀 이미지를 빌드하는 GitHub Actions workflow 를 설계해라. 기존 workflow 를 먼저 읽고 재사용 가능한 부분을 밝혀라. |
| BANK-OM-005 | BANK-OM-005 의 `required_tests` 를 어떻게 고칠지 두 안(source_guard 등재 / 브라우저 test 수정)의 장단점을 비교하고 권고안을 내라. |
| 검토만 | 위 판단 중 근거가 부족하거나 과장된 곳을 지적해라. 특히 T61 3/5 산정과 롤백 차단의 blocking 등급이 타당한지 본다. |
| 롤백 | 순정 롤백 불가에 대한 선택지 세 가지(포기 / 데이터 정리 절차 / `serviceType` 설계 변경)를 각각 구현 범위와 위험으로 평가해라. |

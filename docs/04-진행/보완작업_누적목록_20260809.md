# 보완 작업 누적 목록

> 시작: 2026-08-09 KST · 대화에서 확정된 "해야 할 일"만 누적한다.
> 판단 근거와 실측 수치를 함께 남기고, 완료 시 상태만 바꾼다.

## A. 검사기 보완

| # | 항목 | 근거 | 규모 | 상태 |
|---|---|---|---|---|
| A1 | `upgrade_watch` 출력에 A/M/D 상태 표시(`--name-status`), 삭제·이동 경로를 최상단 분리 | 실측: 1.13.1→1.13.2 실행 시 `approval` 6건 95경로, `EntityUtils.tsx` 삭제가 "paths=26 (e.g. 다른파일)"에 묻힘 | 반나절 | 대기 |
| A2 | 가산 전용 불변식 — 공식 대비 삭제 0 원칙, 삭제 있는 파일만 리뷰 표면으로 | 실측: 공식 1.13.2 대비 +12,347 / −52, 삭제 있는 파일 19개. 경로 비의존이라 파일 분할에 안 깨짐 | 하루 | 대기 |
| A3 | 리뷰 표면 3층 자동 산출 (전체 → 양쪽 부모와 다름 → 공식 코드 삭제 포함) | 실측: 1,707 → 44 → 19. 현재 검사기가 이 계산을 안 함 | 코드 ~20줄 | 대기 |
| A4 | 이동/유실 판정 분리 — 조각 전역 검색 후 `BLOCK`(유실) vs `REVIEW_REQUIRED`(이동, 새 경로 제시) | 실측: `shared_code` BLOCK 4건 전부 이동, 실제 유실 0. 사람이 코드를 열어야 판정됨 | 스키마 변경 + 반례 | 대기 |
| A5 | 정의 이관 워크플로 — 이동 확정 정의를 새 경로로 옮기는 제안 생성 + 승인 | 현재 114개 정의를 손으로 고쳐야 함. "통과시키려 정의를 무르게 고치는" 유인 발생 | A4 위에 얹힘 | 대기 |
| A6 | 심볼 시그니처 대조 — 우리가 참조하는 공식 선언이 바뀌었는지 | PPT 11장 실증: `getCommonAttributesMap()` 제거 → `getEntityTypeName()` 신설. 텍스트 충돌 0, 경로 watch로는 원리적으로 못 잡음 | 큼 | 대기 |
| A7 | 오류 등급 정책표 — 크리티컬/일반 분류를 LLM에서 회수. migration·인증·DB 스키마·기동 실패는 `protected` | PPT 5장은 LLM이 스스로 분류. 실제로 마이그레이션 체크섬으로 서버 기동 실패 발생(11장) | 표 + gate. **등급 승인자 필요** | 대기 |
| A8 | 예측-결과 자동 대조 — premerge 예측을 기계 판독 형태로 SHA에 결속, postmerge에서 일치/불일치/미검증 자동 계산 | PPT 11장: 예측 밖 4건(기동 실패 포함)인데 결론은 "일치". LLM 자기채점 | 양쪽 스키마 | 대기 |
| A9 | postmerge build-success gate | 오늘자 검토반영 P1-3·백로그 1. "text 충돌 0"만으로 다음 단계 불가 | 신뢰 CI 정책 필요 | 대기 |
| A10 | PPT 14개 테스트 → Contract catalog 매핑 | 오늘자 백로그 2. 현재 `✅`가 LLM이 쓴 글자 | 큼. 외부 환경 필요 | 대기 |
| A11 | ID별 소스 수준 `direct_test` 최소 1개씩 | 7개 ID 전부 `direct_tests: 0`. Contract 1개에만 의존하는데 Contract는 런타임 환경 필요로 skip 상태 | 중 | 대기 |
| A12 | T61 patch-kill 나머지 3개 (API 기반 high ID) | 현재 2/5. 이게 채워져야 "Contract 통과 = 커스텀 생존"이 성립 | 런타임 환경 필요 | 대기 |
| A13 | LLM 충돌 해소 패킷 생성 명령 (`conflict-brief`) — 소유 ID·assertion 전문·Contract invariant·Manifest·BASE/OURS/THEIRS·원본 commit·금지 규칙·출력 스키마 | assertion을 미리 주면 "채점 기준 공개한 시험"이 되어 사전에 검증이 걸림 | 기존 데이터 조합 | 대기 |
| A14 | 독립 이중 해소 + 대조 — 두 세션이 서로 모르게 해소하고 트리 diff, 갈린 파일만 사람에게 | 사람 개입을 "모든 충돌"에서 "불일치"로 한정. 토큰 2배 | 중 | 대기 |
| A15 | 컴파일러 강제 설계 적용 지점 조사 (exhaustive `Record`, union 멤버, 필수 인터페이스 구현) | `EntityTypeName: Record<EntityType, string>`은 누락 시 컴파일 실패 → 검사 불필요. 리팩터링 자동 추종 | 조사부터 | 대기 |
| A16 | rename 탐지 이원화 — 지표용 off 유지(`diff.renames=false`), 진단용 on(`-M -C`) 별도 실행 경로 | 현재 지표 결정성 때문에 전부 off라 이동 추적이 구조적으로 불가 | 소 | 대기 |
| A17 | shared 경로 분류 확인 — `elasticsearch/{en,jp,ru,zh}/query_index_mapping.json` 4개가 공유 경로 밖(assertion 없음). 공식 파일이면 재분류 | 제품 저장소 대조 필요 | 확인 | 대기 |

## B. 1.13.2 병합 후보 — 최종 커밋 전

머지 미커밋, `MERGE_HEAD` 유지 중. 임시 검증 커밋 `5262244`(브랜치 무변경).

| # | 항목 | 상태 |
|---|---|---|
| B1 | 삭제(−)가 있는 19개 파일 확인 (1위 `SchemaEditor.test.tsx` −18, 2위 `databaseService.json` −7) | 대기 |
| B2 | `databaseService.json` ↔ `generated/databaseService.ts` ↔ `generated/createDatabaseService.ts` 재생성 일치 확인 (손으로 맞추지 말 것) | 대기 |
| B3 | `query_index_mapping.json`이 en/jp/ru/zh만 있고 ko-kr 없음 — 원래 그런지 확인 | 대기 |
| B4 | `shared-code-definitions.yaml` 4건 갱신: `DatabaseServiceUtils.tsx` → `DatabaseServicePureUtils.ts` (이동 확인 완료, 정의만 낡음) | 대기 |
| B5 | 추가 테스트 4종 — ① 분리된 3개 파일별 BANK 케이스 테스트 ② `databaseSchemaMap` 반례(T61) ③ 스키마↔생성물 일치 ④ 19개 locale 키 존재 | 대기 |
| B6 | 프로덕션 번들 · Docker 런타임 · 화면 검증 (미실행) | 대기 |

### 확인 완료 (재검토 불필요)

- 계보: 2-부모 정상, 공식 1.13.2·커스텀 1.13.1 양쪽 조상 PASS
- `EntityUtils.tsx` 재이식 3곳 적절 — `EntityLinkUtils.ts:47` / `EntityBreadcrumbPureUtils.ts:136` / `EntityNameUtils.ts:151`, 케이스 본문 원본과 동일, 공식 barrel 사용, 옛 구조 부활 없음
- 식별자 인구조사 18종 감소 0건
- BANK-OM-004 `SchemaTable.component.tsx` +82/−0 (No·PrimaryKey·AttributeName·InstanceName)
- Sybase/Tibero 비대칭 3곳(`ServicePureUtils.ts:64` 등)은 1.13.1과 동일한 기존 상태
- 등록 경로 112개 중 소멸은 `EntityUtils.tsx` 1건뿐이며 공식이 삭제한 것

## C. 기록·정리

| # | 항목 | 상태 |
|---|---|---|
| C1 | 오늘 검토 결과를 `CODEX_CURRENT_HANDOFF.md` 8/9자 항목으로 반영 | 대기 |
| C2 | 미커밋 실행 증거 2개 디렉터리 처리 — `evidence/om-1.13.2-premerge-20260809-premerge-01/`, `harness/registrations/om-temp-1.13.1/candidate-locks/` (02:43~03:08 생성, 마지막 커밋 이후) | 대기 |

## D. 작업 환경

| # | 항목 | 상태 |
|---|---|---|
| D1 | 무거운 작업은 하나씩 — 빌드 중 colima 내리고, Docker 중 빌드 금지. 스왑 13GB 사태의 직접 원인 | 상시 |
| D2 | 무거운 작업 전 스왑·디스크 확인, 이미 커져 있으면 먼저 재부팅 | 상시 |
| D3 | 기준선 대비 타입 오류 비교 — 공식 1.13.2 기준선 1,339줄을 저장해두고 차감 | 진행 중 |

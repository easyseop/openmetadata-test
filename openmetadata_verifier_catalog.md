# 검증기 카탈로그 — 무엇을 만들고, 왜, 무엇을 잡나

> **이 문서의 용도**
> 거버넌스 하네스가 만들 **모든 검증기**를 한 곳에 정리한다. 검증기마다
> "무엇을 잡나 / 없으면 어떤 사고가 나나 / 못 잡는 것 / 해당 개발 태스크"를 적었다.
> 나중에 README에 그대로 넣을 수 있게 자립형으로 작성한다.
>
> **선행**: 개발 실행 계획(`openmetadata_build_plan.md`)의 태스크 번호(T##)와 대응.

---

## 0. 큰 그림 — 검증기는 3계층 + 보조

모든 검증기는 하나의 목표를 위해 있다.

> **"우리 수정이 빠짐없이 · 규칙대로 · 재현 가능하게 새 버전에 올라갔다"를
> 사람 기억이 아니라 기계가 보장한다.**

그런데 이 목표가 뚫릴 수 있는 구멍이 여러 개라, 검증기 하나가 구멍 하나씩을 막는다.

| 계층 | 담당 | 보장 성격 |
|---|---|---|
| **계층 1 — 결정적 구조** | 등록·재적용 완전성 (git·경로) | 확실 (기계 판정) |
| **계층 2 — 증거 생성기** | 구조 변화 사실 추출 | 확실 (사실만, 판단은 사람/테스트) |
| **계층 3 — 테스트** | 동작·의미 | 명시된 범위 내 확실 |
| **계층 4 — 무결성·정책·집계** | 재현성·자기보호·판정 | 확실 |
| **보조 — LLM** | 의미 변경 후보 제시 | 보조 (판정 아님) |

**핵심 경계**: 계층 1·2·4는 "등록·재적용·구조"까지 결정적으로 보장하고,
"기능 의미"는 계층 3(테스트)이 담당한다. LLM은 그 사이의 후보만 좁혀준다.

---

## 계층 1 — 결정적 구조 검증기 (등록·재적용 완전성)

### 1. 재적용 게이트 (T20 / T21)
- **뭘 잡나**: 우리 패치를 새 버전 위에 다시 얹을 때 적용 성공 여부(물리 충돌 유무).
- **막는 사고**: 수정 하나가 안 얹힌 채 조용히 진행 → 몇 주 뒤 "이 기능 왜 안 되죠?".
- **못 잡는 것**: 얹히긴 했으나 로직이 틀린 경우(→ 테스트), 충돌 없는 의미 변경(→ ⑨·테스트).

### 2. 커밋 단위 불변식 (T30)
- **뭘 잡나**: 이름표(ID) 없는 코어 커밋, 한 커밋에 여러 ID, merge·empty 커밋, core+governance 혼합.
- **막는 사고**: 이름표 없이 원본을 고치거나 수정을 뭉치면 **"우리가 뭘 바꿨나"를 셀 수 없게** 됨 → 누락 검증 자체가 무의미해짐.
- **못 잡는 것**: 커밋이 규칙은 지켰으나 내용이 틀린 경우.

### 3. ID·series 불변식 (T31)
- **뭘 잡나**: 한 수정이 여러 커밋(서버+UI+테스트)일 때 순서·개수 누락, 의존 순환, retired ID 재사용, 비연속 분산.
- **막는 사고**: 한 수정의 **절반만 반영**돼도 통과.
- **못 잡는 것**: series가 다 있으나 로직 결함.

### 4. 최종 상태 불변식 (T32)
- **뭘 잡나**: 패치 순효과 0(뒤에서 revert·덮어씀으로 실제 변화 없음), active ID의 무단 revert.
- **막는 사고**: **"이름표는 다 있는데 기능은 사라진"** 상태를 통과(GPT P0-5 핵심).
- **못 잡는 것**: 순효과는 있으나 의미가 틀린 경우(→ 테스트).

### 5. clean-room replay (T22)
- **뭘 잡나**: candidate가 정말 "고정 공식버전 + patch-lock 패치들"로만 재현되는지(재생 결과 == candidate tree).
- **막는 사고**: 검증받지 않은 **은밀한 변경**이 섞임 / 재현 불가로 신뢰 붕괴.
- **못 잡는 것**: 재현은 되나 패치 내용 자체가 틀린 경우.

### 6. 구현범위 drift (방향 C) (T40)
- **뭘 잡나**: 명세 선언(allowed/required_changed_paths)과 실제 변경 파일의 불일치.
- **막는 사고**: 명세가 실제와 어긋나 **믿을 수 없게** 됨 → 뒤의 영향 분석·감시가 무력화.
- **못 잡는 것**: 선언과 실제가 일치하나 의미가 틀린 경우.

### 7. 민감영역·의도 게이트 (T41)
- **뭘 잡나**: 인증·마이그레이션 등 민감 경로 접촉, 선언 범위 이탈(scope creep·forbidden).
- **막는 사고**: 위험 변경이 **검토 없이** 통과.
- **못 잡는 것**: 민감 영역 안에서의 의미적 오류(→ 테스트).

### 8. 민감·감시경로 drift (T93)
- **뭘 잡나**: 민감/감시 glob이 새 버전에서도 실제 경로를 ≥1 매칭하는지(정책 노후화).
- **막는 사고**: 업스트림 리팩터로 코드가 이사가면 정책이 **빈 총**이 되는데도 "통과"만 뜸.
- **못 잡는 것**: 패턴은 매칭하나 분류가 부적절한 경우(사람 리뷰).

### 9. 업그레이드 영향분석 (upgrade_watch) (T42)
- **뭘 잡나**: 우리가 **의존한다고 선언한** 파일·설정키·의존성이 업스트림에서 바뀜(케이스 D).
- **막는 사고**: 우리가 편집 안 한 의존 대상(예: `UserContext`의 tenant 추가) 변경을 놓침.
- **못 잡는 것**: 선언 안 한 깊은 전이 의존(→ 테스트), 그 변경이 실제 깨는지의 판단(→ 테스트).

### 10. 부채 게이트 (T43)
- **뭘 잡나**: 코어 수정 개수·변경량·반복 충돌률·hotspot 겹침 등이 상한 초과.
- **막는 사고**: 코어 수정이 서서히 쌓여 **업그레이드 불가능한 포크**로 붕괴.
- **못 잡는 것**: 개별 패치의 옳고 그름(양적 지표일 뿐).

### 11. patch-lock 일치 (T11)
- **뭘 잡나**: 재적용 소스가 **고정 SHA**(patch-lock) 기준인지, 동적 "최신 브랜치" 조회를 하지 않는지.
- **막는 사고**: 업그레이드 도중 소스가 바뀌어 **재현성이 깨짐**(같은 릴리스가 매번 달라짐).
- **못 잡는 것**: lock에 적힌 SHA 자체가 잘못 선택된 경우(사람 판단).

---

## 계층 2 — 증거 생성기 (결정적, LLM 이전)

### 12. 선언형 verifier (T50)
- **뭘 잡나**: config/deployment/extension이 실제 반영됐는지(Helm 값·이미지 내 패키지·import·설정키). 타입: `yaml_value_equals`·`helm_jsonpath_equals`·`file_exists_in_image`·`python_import_succeeds`·`api_schema_contains`·`package_version_equals`.
- **막는 사고**: 코어가 아닌 설정·확장이 실수로 되돌려져도 완전성 게이트가 못 잡음(그건 커밋만 봄). / manifest에 임의 shell을 쓰면 **CI 권한 임의 코드 실행**(GPT P0-8).
- **못 잡는 것**: 반영은 됐으나 값의 의미가 틀린 경우(→ 테스트).

### 13. 구조화 diff providers (T51 / T52)
- **뭘 잡나**: API/JSON Schema, Helm·설정키, 의존성·SBOM, DB migration·schema, 검색 mapping의 **구조 변화 사실**.
- **막는 사고**: 반환 구조·설정 기본값·스키마 변경 같은 의미 변경의 **근거를 사람·LLM보다 먼저 결정적으로** 확보하지 못함.
- **못 잡는 것**: 그 구조 변화가 우리 업무를 깨는지의 **판단**(→ 사람·테스트).

---

## 계층 3 — 동작·의미 검증 (테스트)

### 14. 필수 테스트 존재 검사 (T하위)
- **뭘 잡나**: 명세의 테스트 ID가 카탈로그에 실재하는지.
- **막는 사고**: 존재하지 않는 테스트를 "필수"로 걸어두고 검증했다고 착각.
- **못 잡는 것**: 테스트가 실제로 유효한지(→ patch-kill).

### 15. contract-id ↔ 테스트 결속 (T60)
- **뭘 잡나**: 업무 불변식(예: "userId 같아도 tenant 다르면 다른 사용자")이 특정 테스트로 입증되는지.
- **막는 사고**: 릴리스마다 테스트 **이름만** 확인하고 "어떤 업무 규칙이 지켜지는지"는 모른 채 넘어감.
- **못 잡는 것**: 불변식 목록에 아직 없는 새 규칙.

### 16. patch-kill test (T61)
- **뭘 잡나**: 그 패치를 빼면 해당 required 테스트가 **실제로 실패하는지** = 테스트가 커스터마이징 생존을 입증하는지.
- **막는 사고**: 패치를 제거해도 테스트가 통과 → 그 테스트는 **생존을 증명하지 못하는 껍데기**인데 그걸 모름.
- **못 잡는 것**: 테스트가 없는 새 의미 차원.

### 17. 테스트-candidate SHA 결속 (T62)
- **뭘 잡나**: 테스트 결과가 정확한 candidate SHA·이미지 digest에 묶였는지. 재시도 결과 구분(first/retry/flaky/fail).
- **막는 사고**: 테스트 통과 후 candidate에 커밋이 추가돼도 **옛 결과를 유효로** 착각 / flaky retry-pass를 성공으로 뭉갬.
- **못 잡는 것**: 테스트 자체의 커버리지 부족.

### 18. 업그레이드 차등 테스트 (T90)
- **뭘 잡나**: 구·신 버전에 같은 데이터·요청을 넣어 인증·권한·API 응답·엔티티 관계·검색·ingestion 결과가 달라졌는지.
- **막는 사고**: 건수 대사만으로는 **관계·의미 손상**을 놓침.
- **못 잡는 것**: 후보 환경이 재현 못 하는 운영 부하·외부 연계 차이(→ 운영 관찰).

---

## 계층 4 — 무결성 · 정책 · 집계

### 19. 정책 PR base-평가 (T70)
- **뭘 잡나**: 정책·게이트·CI 경로를 완화하는 PR이 **완화된 새 정책으로 자기 자신을** 통과시키려는 것.
- **막는 사고**: 승인 요건을 스스로 없애는 **자기 완화 우회**(GPT P0-9).
- **못 잡는 것**: 저장소 밖 플랫폼 통제(→ required check·branch protection 병행).

### 20. digest 승격 무결성 (T91)
- **뭘 잡나**: 검증한 것과 배포되는 것의 commit SHA·artifact digest 일치(재빌드·복사 금지).
- **막는 사고**: 검증본과 **다른 산출물**이 배포되는 것 / 외부망-내부망 불일치.
- **못 잡는 것**: digest는 같으나 그 산출물 자체가 잘못 검증된 경우(→ 상위 게이트).

### 21. verdict 엔진 (T13)
- **뭘 잡나**: (판정기) 게이트 결과 집계 시 block과 approval의 역전 방지, 분석 실패=차단.
- **막는 사고**: `max()` 집계로 **차단이 승인필요로 격하**(GPT P0-3) / 검사기 고장을 승인으로 우회(GPT P0-4).
- **못 잡는 것**: 개별 게이트의 판정 옳고 그름(각 게이트 책임).

---

## 보조 — LLM (판정 아님)

### 22. LLM Upgrade Impact Memo (T80)
- **뭘 하나**: 충돌·구조 변화가 애매한 **의미 변경 후보**를 릴리스노트+명세+증거 provider로 제시.
- **막는 사고에 기여**: 케이스 D처럼 충돌 없는 의미 변경을 사람이 **놓치기 전에 검토건으로** 좁혀줌.
- **한계·못 하는 것**: pass 부여 불가, 100% recall 아님, 확정은 테스트. "영향 없음" 금지("확인된 후보 없음"으로).

---

## 케이스 A~E를 누가 잡나

```
A 무관한 변경        → ⑥ 경로 대조 (무관 판정)
B 우리 파일·다른 줄   → ① 재적용 자동 + ⑦ 경로 플래그
C 같은 줄 충돌        → ① 재적용 게이트 (물리 충돌)
D 의존 대상 변경      → ⑨ upgrade_watch + ⑬ 구조화 diff + 22 LLM → (계층3) 테스트 확정
E 깊은 의존·의미 붕괴 → (계층3) 테스트만: 16 patch-kill · 15 contract · 18 차등 테스트
```

앞쪽 계층으로 갈수록 구조적 케이스(A~C), 뒤로 갈수록 의미적 케이스(D~E)를 담당한다.

---

## 한 장 요약 — 검증기 × 막는 사고

| # | 검증기 | 막는 사고 (한 줄) | 계층 | 태스크 |
|---|---|---|---|---|
| 1 | 재적용 게이트 | 수정이 안 얹힌 채 진행 | 1 | T20/21 |
| 2 | 커밋 불변식 | 셀 수 없게 돼 누락 검증 무의미 | 1 | T30 |
| 3 | ID·series 불변식 | 한 수정의 절반만 반영 | 1 | T31 |
| 4 | 최종상태 불변식 | 이름표는 있는데 기능은 사라짐 | 1 | T32 |
| 5 | clean-room replay | 은밀한 변경 섞임 / 재현 불가 | 1 | T22 |
| 6 | 구현범위 drift | 명세가 실제와 어긋나 믿을 수 없음 | 1 | T40 |
| 7 | 민감·의도 게이트 | 위험 변경이 검토 없이 통과 | 1 | T41 |
| 8 | 경로 drift | 정책이 빈 총인데 통과만 뜸 | 1 | T93 |
| 9 | upgrade_watch | 의존 대상 변경(tenant) 놓침 | 1 | T42 |
| 10 | 부채 게이트 | 업그레이드 불가 포크로 붕괴 | 1 | T43 |
| 11 | patch-lock 일치 | 소스가 흔들려 재현 불가 | 1 | T11 |
| 12 | 선언형 verifier | 설정·확장 반영 누락 / 임의 실행 홀 | 2 | T50 |
| 13 | 구조화 diff | 의미 변경 근거를 결정적으로 못 확보 | 2 | T51/52 |
| 14 | 필수 테스트 존재 | 없는 테스트를 검증했다고 착각 | 3 | CG |
| 15 | contract 결속 | 어떤 업무 규칙이 지켜지는지 모름 | 3 | T60 |
| 16 | patch-kill | 생존 못 증명하는 껍데기 테스트 | 3 | T61 |
| 17 | 테스트-SHA 결속 | 옛 결과를 유효로 착각 / flaky 뭉갬 | 3 | T62 |
| 18 | 차등 테스트 | 관계·의미 손상을 건수 대사로 놓침 | 3 | T90 |
| 19 | 정책 base-평가 | 자기 완화 우회 | 4 | T70 |
| 20 | digest 승격 | 검증본과 다른 산출물 배포 | 4 | T91 |
| 21 | verdict 엔진 | 차단이 승인으로 격하 / 고장 우회 | 4 | T13 |
| 22 | LLM Memo | (보조) 의미 변경 놓침 전에 후보 제시 | 보조 | T80 |

# 경영진 요약 — OpenMetadata 업그레이드 커스터마이징 검증 체계

> 5분 안에 결정에 필요한 것만. 기술 용어 없이. 상세는 개발자 문서를 본다.
>
> 상태: 구현 진행 · 대상: 승인권자·경영진 · 기준 커밋: 이 브랜치 최신 · 최종 갱신: 2026-07-25

## 1. 문제

우리는 오픈소스 데이터 카탈로그(OpenMetadata)를 행내 환경에 맞게 일부
수정해서 쓴다. 오픈소스가 새 버전을 내면 **우리 수정이 새 버전에 다시
올라가야** 하는데, 지금은 그 과정이 사람 기억과 수작업에 의존한다. 그래서
세 가지 사고가 반복될 수 있다.

- 수정 하나가 **빠진 채** 배포되어 몇 주 뒤 "이 기능 왜 안 되죠?"
- 그 수정을 **왜 했는지 기록이 사라져** 되살리지 못함
- 위험한 변경이 **검토 없이** 지나감

## 2. 선택한 전략

행내 수정을 **고유 번호가 붙은 기능·계약**으로 관리하고, 공식 OpenMetadata와
공통 이력을 유지하는 vendor branch에 승인된 공식 버전을 **merge**한다. 자동
검사는 공식 버전의 통합, 행내 수정의 생존, 위험 변경과 기능 동작을 판정한다.

cherry-pick 재적용은 필수 운영 방식이 아니라 이식성 확인·복구·다중 버전 지원을
위한 선택 진단 수단으로 사용한다.

## 3. 자동으로 **보장하는 것**

- 승인된 공식 버전이 vendor candidate에 들어왔고, 모든 행내 수정이 **등록·생존**
  하는지 자동 검사가 확인한다.
- 검토 없이 들어온 **범위 밖 변경**과 **민감 영역**(인증 등) 접촉을 차단한다.
- 배포되는 산출물이 **검증한 바로 그것인지**(뒤바뀌지 않았는지) 확인한다.

## 4. 자동으로 **보장하지 않는 것** (정직하게)

> 모든 행내 수정이 등록되고 candidate에 남아 있는지는 자동 검사가
> 검증한다. 다만 **충돌이 없는 의미 변화**까지 이력만으로 100% 보장한다고
> 주장하지 않는다. 이 부분은 업무 규칙 테스트·업그레이드 검증·담당자 리뷰·운영
> 관찰을 결합해 **잔여 위험으로 관리**한다.

즉 "등록·통합 후 남아 있다"와 "기능이 실제로 올바르게 동작한다"는 **다른 층**이며,
후자는 테스트와 운영 검증이 담당한다.

## 5. 1차 도입 범위와 제외 범위

| 현재 구현 | 실제 운영에서 남은 단계 |
|---|---|
| 공식 1.13.1 기반 실제 7-ID vendor candidate와 source 누락 검사 | 조직 owner·2인 승인 경로 지정 |
| 7개 업무 계약 구현 존재와 candidate-bound runtime 실행기 | API 4개·browser IME 1개 실제 환경 통과 |
| 변경 범위·민감 경로·업스트림 영향 후보 통제 | 실제 DB·검색·ingestion 업그레이드 실행 |
| 릴리스·내부망 반입 결과계약과 검증기 | 실제 artifact/SBOM/서명·승격·반입 증거 |

## 6. 필요한 역할

- **통합 담당자(1인)**: upstream merge·충돌 해결 반영, 기준 잠금 갱신.
- **커스터마이징 작성자**: 수정마다 명세·민감경로·테스트 선언.
- **승인권자**: 민감 변경·예외 승인.
- 반복 가능한 검사는 자동화하고, 사람은 **초기 기준 설정과 위험·예외 판단**에 집중한다.

## 7. 업그레이드마다 발생하는 운영 부담

- 승인된 공식 SHA를 vendor branch에 merge하고 자동 검증을 실행한다.
- 충돌과 검토 항목은 별도로 처리하며, 커스터마이징이 적고 격리될수록 부담이 줄어든다.

## 8. 현재 상태

| 영역 | 상태 |
|---|---|
| replay 방식의 패치 등록·재적용 검증 | 구현 완료(선택 모드) |
| candidate SHA·tree·artifact 잠금 | 구현 완료(T24) |
| vendor merge ancestry·공식 target 포함 검증 | 구현 완료(T25) |
| customization 생존·실제 등록 검증 | 실제 재구축 candidate에서 T25/T26 통과(원본 snapshot 단독 사용은 차단) |
| 변경 범위·민감 경로 검증 | 구현 완료 |
| 업스트림 영향 분석 | 구현 완료(후보 표시) |
| 업무 계약 실행·후보 결속 | 실행기·별도 runtime workflow 완료, 실제 run 필요 |
| 실제 DB·검색 업그레이드 검증 | 결과계약 구현·실제 실행 필요 |
| 내부망 반입·릴리스 승격 | 검증기 구현·실제 승격/서명 필요 |

(현재 검증 298개 중 293개 통과. API 4개와 실제 브라우저 IME 1개는 필요한
운영 환경이 없어 skip이며 통과로 계산하지 않는다.)

## 9. 주요 잔여 위험

- **충돌 없는 의미 변화**(우리가 의존하는 동작이 조용히 바뀜) — 감시·테스트·리뷰로 관리.
- **선언 안 한 깊은 의존** — 테스트로만 포착.
- 수용 책임자: (승인권자 지정 필요).

## 10. 이번 심의에서 받을 결정

1. 이 전략(vendor merge 기본 + 자동 검사)을 표준으로 채택할지.
2. 1차 도입 범위(위 5장)를 승인할지.
3. 통합 담당자·승인권자 역할을 지정할지.
4. 잔여 위험(4·9장)의 수용 책임자를 정할지.

## 11. 성공 지표(예시)

- 업그레이드당 누락으로 인한 사후 사고 건수 = 0
- 업스트림 merge 후 자동 검증으로 처리되는 커스터마이징 비율
- 민감 변경이 검토 없이 배포된 건수 = 0

---
*이 문서는 의사결정용 요약이다. 기술 상세는 `README.md` → 개발자 문서를 본다.*

# OpenMetadata 커스터마이징 거버넌스 — 대화 요약 및 검토 요청

> **이 문서의 목적**
> OpenMetadata를 행내에 도입하며 "버전 업그레이드 시 커스터마이징을 빠짐없이·검증
> 가능하게 반영"하기 위한 전략을 설계한 논의의 요약이다. 외부(예: GPT) 검토를 받기
> 위해, 결정 사항과 근거, 그리고 아직 열린 질문을 한 문서로 정리했다.
>
> **함께 보는 문서**
> - 상세 설계: `openmetadata_upstream_customization_design.md`
> - 전략 브리핑: `openmetadata_strategy_briefing.md`
> - 요구사항 정의서: `openmetadata_governance_requirements.md`
> - 시나리오·개요(시각): `om-overview-share.html`

---

## 1. 목표

오픈소스 OpenMetadata를 행내에 도입하되:
- 행내 커스터마이징을 **최소한**으로 유지하고,
- 공식 버전이 올라가도 커스터마이징을 **즉시, 빠짐없이 재적용**하며,
- "빠짐없이 반영됐는지"를 **사람 기억이 아니라 기계(CI)가 검증**하게 한다.

배경: 상급자가 "벤더브랜치 전략이 잘 돌아가는지 어떻게 검증할 거냐"고 물었고,
이 질문에 답하는 것이 설계의 출발점이다.

---

## 2. 핵심 전략 결정 — merge가 아니라 패치 스택

### 결정
순수 벤더브랜치(merge 기반)를 버리고, **패치 스택(patch stack)** 방식을 채택.
즉 커스터마이징을 **이름표(ID) 붙인 독립 커밋**으로 유지하고, 새 공식 태그 위에
**cherry-pick으로 재적용**한다.

### 근거
- merge 방식은 커스터마이징이 병합 기록에 흩어져 **"다 반영됐나"를 기계적으로
  셀 수 없다** → 상급자 질문에 정직하게 답하면 "사람이 확인"밖에 안 됨.
- 패치 스택은 커밋 개수를 셀 수 있어 **누락을 CI 실패로 검출** 가능.
- 이 방식은 Debian/Fedora 패키지 패치, Android 벤더 포크 등에서 표준적으로 쓰임.

### 브랜치 구조
```
Origin (공식, 미러 · 태그 고정: v1.5, v1.6 …)
   │  새 태그 위에 재적용 (cherry-pick, Merge 아님)
   ├─ 코어 패치 (원본 수정, 이름표 O, 최소화): BANK-OM-001/002/003
   └─ 확장 모듈 (원본 안 건드림, 별도 코드): 커넥터·리포트·UI
   ↓  자동 검사(게이트) 통과해야
Release: candidate/om-1.6-bank.1 → release/om-1.6-bank.1 (재현·검증 가능)
```
- 재적용 정본 규칙: cherry-pick 소스는 항상 **직전 릴리스 브랜치의 최신 리비전**.
  (핫픽스가 다음 버전으로 유실되지 않게)

### 충돌에 대한 핵심 고려사항 (운영 시 반드시 인지)

- **충돌은 방향·순서가 아니라 "겹침"이 만든다.** 공통 조상(원본) 기준 3자 비교이므로,
  새 버전 위에 코어 패치를 얹든(cherry-pick) 새 버전을 커스텀에 병합하든(merge),
  우리 코어 수정과 업스트림이 **같은 줄**을 다르게 바꿨으면 반드시 충돌한다.
  순서를 "업스트림 먼저 → 코어 수정 적용"으로 바꿔도 동일하다.
- **확장 모듈·설정(별도 파일)은 원본과 안 겹치므로 충돌이 없다.** 충돌은 오직
  **코어 수정(원본 파일 편집)**에서만 발생한다.
- 따라서 **충돌 최소화 = 코어 수정 최소화**다. 가능한 요구는 전부 확장·설정으로
  빼고, 코어 수정 브랜치는 얇게 유지한다. (4단계 관문·부채 상한 게이트의 목적)
- git은 삭제/추가를 아무 데나 적용하지 않는다. 삭제 대상이 원본과 안 맞으면
  조용히 붙이지 않고 **충돌로 세운다**(업스트림 변경 유실 방지 안전장치).

---

## 3. 안전장치 — 4개 자동 게이트 (LLM 불개입, 결정적)

판정은 전부 exit code·집합 비교·카운터 등 결정적 연산. 언어 무관(코드 내부를
파싱하지 않고 변경 이력·파일 경로·YAML 명세만 사용).

1. **재적용 게이트** — 모든 패치가 새 버전에 적용됐는가 (cherry-pick 종료코드)
2. **완전성 게이트** — 명세 ID 집합 = 커밋 ID 집합인가 (누락·미등록 검출) + 방향 C
   (커밋 실제 변경파일 ↔ 명세 affected_paths drift 검사)
3. **rerere 검출 게이트** — 자동 충돌 해결이 개입하면 테스트·리뷰 강제 (기본 비활성)
4. **부채 상한 게이트** — 코어 패치 수·라인·연속 충돌이 상한 초과 시 재설계 요구

---

## 4. 커스터마이징 유형과 4단계 관문 (원천 억제)

새 요구사항은 순서대로 통과해야 코어 수정이 허용된다. 각 관문의 판단은 감이 아니라
정해진 확인 대상(공식 설정 목록 / Helm·프록시 / 확장 SDK 유무)으로, 실제 검증 환경
PoC로 확정하고 근거를 ADR에 남긴다.

```
① 공식 설정으로 가능?  → 설정
② 배포 설정(Helm·프록시)로 가능? → 배포
③ 확장 모듈(커넥터·어댑터)로 가능? → 확장
④ 전부 불가 → 코어 수정 (ID·테스트·제거조건·ADR 필수, CI가 강제)
```
- "코어 수정" = 오픈소스 원본 소스 파일 자체를 편집하는 것. 업그레이드 부담은
  **전부 ④에서만** 발생하므로 ④를 최대한 걸러낸다.

---

## 5. 업그레이드 영향 분석 — 커밋 비교 + 3층

릴리즈는 베이스가 통짜로 바뀌는 게 아니라 **연속 이력 위의 태그**이므로,
`git diff 구버전 신버전`으로 커밋 단위 비교가 가능하다(우리 영향 분석의 토대).
영향은 3층으로 나뉜다.

| 층 | 상황 | 자동 검출 |
|---|---|---|
| ① 직접 겹침 | 우리가 고친 파일을 공식도 고침 | 가능 (비교·충돌) |
| ② 경로 근접 | 공식 변경이 우리 영향 경로에 걸림 | 가능 (경로 대조) |
| ③ 간접 영향 | 위치는 안 겹치는데 동작·설정 변화가 우리 로직을 깸 | 비교만으론 어려움 |

역할 분담: **커밋 비교 = 무엇이 바뀌었나(확실)**, **테스트 = 동작 확정**,
**LLM = ③의 후보 제시(보조)**.

---

## 6. 가장 어려운 케이스와 방어 (검토 중점)

**케이스**: 충돌도 안 나고 컴파일도 되는데, 의미가 달라져 커스터마이징이 조용히
깨지는 경우. 예 — 공식이 신원을 `userId` 하나에서 `userId + tenantId` 둘로 분리.
우리 SSO 매핑은 `userId`만 읽으므로 매핑이 불완전해짐.

**무엇이 놓치나**: 충돌 검출·완전성 게이트(구조·존재만 봄) → 놓침.

**무엇이 잡나 (겹겹 방어)**:
1. **최소화** — 코어를 적게 건드릴수록 이런 표면이 적음.
2. **테스트(1차 안전망)** — 코어 패치의 필수 테스트가 "올바른 신원 매핑"을 검증하면
   업그레이드 후 그 테스트가 실패로 잡음. (그래서 코어 패치에 테스트 의무화)
3. **릴리스 노트 리뷰(사람+LLM)** — "새 차원(tenant)"처럼 테스트가 모르던 것은,
   릴리스 노트를 읽고 연결하는 데서 잡음. **LLM 위키의 대표 쓸모가 바로 여기**.
4. **업그레이드 검증**(건수 대사·E2E)로 다운스트림 이상 포착.

**정직한 한계**: "충돌 없고 예상 못 한 의미 변경"은 **어떤 단일 자동 장치도 100%
보장 못 함** — 진화하는 오픈소스를 따라가는 모든 방식의 근본 한계. merge 방식은
자동 병합으로 오히려 더 숨김. 우리는 겹겹 방어로 확률을 낮춘다.

### 6.1 케이스 ② — 텍스트 충돌 없는 의미 붕괴 예시

우리가 건드린 줄(텍스트)은 그대로라 git이 겹침을 못 본다(cherry-pick 충돌 0).
깨지는 것은 "우리가 **의존하는 것**의 의미"다. 테스트·LLM 검토건이 메운다.

| 유형 | 우리 코드(그대로) | 공식 변경(다른 곳) | 왜 깨지나 |
|---|---|---|---|
| 반환값 의미 | `if (getUserId()==null) reject()` | `getUserId()`가 null 대신 `""` 반환 | null 체크가 항상 false → 신원 없는 사용자 통과 |
| 필드 분리 | `getUserId()`만 읽음 | 신원 = userId → userId+tenantId | tenant 무시 → 다른 사용자를 같은 사람으로 |
| enum 추가 | `if (role==ADMIN)` | Role에 SUPER_ADMIN 추가 | 신규 관리자 역할을 놓침 |
| API 구조 | `resp.get("owner")` | owner(단일) → owners(배열) | null 파싱 → 소유자 정보 유실 |
| 기본값 변경 | 정책 미명시 시 기존 "공개" 기본 | 기본을 "비공개"로 변경 | 코드 그대로인데 가시성 반대로 |

공통점: 텍스트 충돌이 없으므로 cherry-pick·완전성 게이트로는 못 잡는다.
→ 테스트(동작 확정) + LLM 검토건(릴리스 노트의 "반환값/필드/기본값 변경"을 우리
의존성과 연결)로 방어.

---

## 7. LLM 위키의 역할과 한계

- **역할**: 게이트가 못 읽는 "의미·맥락·역사"를 사람에게 풀어주는 **해설자**.
  대표 쓸모 = **충돌 없는 의미 변경(③)을 '검토건'으로 자동 생성**.
- **동작 방식**: 전체 코드 풀스캔이 아니라, **바뀐 부분(diff) + 릴리스 노트 +
  우리 명세**를 읽고 관계를 추론해 후보를 제시. (필요 시 관련 코드만 검색-RAG)
- **경계**: '제안·설명'만 한다. **통과/차단 판정은 절대 하지 않고** 자동 검사가 한다.
  그래서 LLM이 틀려도 배포 안전에는 영향이 없다. 원본(명세·git)과 다르면 재생성.
- **구현 가능성**: 기존 LLM·RAG 기술로 구현 가능(새 연구 불필요). 단 (1) 정확도
  보장이 아니라 보조 품질, (2) 앞 단계(명세·ID)가 갖춰진 뒤 얹어야 함 → **마지막 단계**.
- **필수 여부**: 없어도 시스템(비교+경로+테스트)은 안전. ③ 꼬리를 효율적으로 좁혀주는
  **선택적 도구**.

---

## 8. 테스트 계층 (별도 작업, 게이트와 다른 층)

- 가져다 씀: OpenMetadata 공식 테스트·공식 마이그레이션 도구.
- 우리가 만듦: 행내 전용 테스트(SSO·커넥터·권한)와 업그레이드 검증 절차(운영 DB 복원
  → 마이그레이션 → 건수 대사 → 재색인 → 확인).
- 게이트 하네스의 범위 밖이며, 설계 로드맵의 후속 단계(Phase 4).

---

## 9. 전체 운영 시나리오 (1.5 → 1.6, tenant 분리 예시)

```
[사전 준비 · 사람 · 1회성]
 S1 수정을 이름표 커밋으로 분해 → release/om-1.5-bank.1 (BANK-OM-001/002/003)
 S2 인증·마이그레이션 폴더를 민감 영역으로 등록
 S5 명세 작성 (의존·필수테스트·담당)

1) 공식 1.6 접수·커밋 비교 [자동]
   미러에 v1.6 받고 git diff v1.5..v1.6 → 인증 파일에 tenantId 추가 감지 (충돌 아님)
2) 영향 분석 [자동+LLM]
   자동: 바뀐 파일 ↔ 영향 영역 대조
   LLM: "신원이 두 필드로 분리 + BANK-OM-001은 userId만 매핑 → 검토·테스트 보강" 검토건
3) 후보 브랜치 생성·재적용 [자동+사람]
   candidate/om-1.6-bank.1 을 v1.6 위에 생성, 직전 릴리스에서 3건 cherry-pick
   002 자동 · 001·003 충돌 → 담당팀이 해결 (병합 아님, 재적용)
4) 묶음 비교·게이트 [자동]
   range-diff로 1.5 스택 ↔ 1.6 스택 대조 + 완전성·재적용·민감영역·부채 게이트
5) 검토·테스트 [사람+자동]
   LLM 검토건 확인 → tenant 매핑 추가 + 테스트 보강 → TEST-SSO 전체 통과 → 동작 확정
6) 릴리스 승격 [자동]
   candidate → release/om-1.6-bank.1, 태그 om-1.6-bank.1, 해시·목록 고정 → 내부망 재현
```
역할: 사람(사전셋팅·충돌해결·검토) / 자동(비교·재적용·게이트·릴리스) / LLM(검토건 보조)
/ 테스트(최종 확정). LLM이 놓쳐도 테스트가 잡으므로 안전 유지.

---

## 10. 검토 요청 포인트 (GPT에게)

아래를 특히 비판적으로 봐 주길 요청한다.

1. **전략 선택** — merge 대신 패치 스택을 택한 근거가 타당한가? 패치 수가 많아질 때의
   cherry-pick 비용 vs merge 비용 트레이드오프를 우리가 과소평가하지 않았나?
2. **완전성 검증의 실효성** — "명세 ID = 커밋 ID" 집합 비교로 누락을 잡는다는 주장에
   구멍이 있나? (예: 한 커밋에 여러 논리 변경이 섞이면?)
3. **가장 어려운 케이스(6장)** — 충돌 없는 의미 변경에 대한 겹겹 방어가 현실적으로
   충분한가? 놓칠 수 있는 경로가 더 있나?
4. **LLM 위키의 위치** — 보조로만 두고 판정에서 배제한 설계가 맞나? 과신·환각을 막는
   경계가 충분한가?
5. **언어 무관 접근의 한계** — 코드 내부를 파싱하지 않는 게이트가 놓치는 위험 유형이
   무엇이고, 테스트/LLM/사람 리뷰로 그 공백이 실제로 메워지나?
6. **운영 부담** — 이 절차가 무거워 현장에서 우회될 위험은 없나? 경량 경로(config 한 줄
   변경 등)가 충분히 가벼운가?
7. **텍스트 충돌 없는 의미 붕괴(6.1)** — 제시한 5개 유형 외에 OpenMetadata에서
   자주 생길 유형이 더 있나? 테스트·LLM 외에 결정적으로 잡을 방법이 있나?
8. **코어 수정 최소화의 현실성** — "충돌은 코어 수정에서만 난다 → 최소화"라는
   방침이 실제 행내 요구를 감당할 수 있나? 확장으로 못 빼는 코어 요구의 비중은?
9. **누락된 관점** — 우리가 아예 고려하지 못한 실패 모드나 운영 리스크가 있나?

# 2026-08-14 오전 12:14 이후 발표자료 문구 수정 요청

## 사용 방법

- 현재 로컬 HTML이 최신 덱이 아닐 수 있으므로, 최신 TO-BE 덱을 찾은 뒤 아래 문구만 재적용한다.
- 아래에는 **오전 12:14 이후 사용자가 요청한 문구 수정만** 기록했다.
- 전체 발표 구성, 이전 날짜의 요구사항, 최신 커밋 탐색 과정은 포함하지 않았다.

## 1. 3페이지 — 검사 방식 설명을 관리 파일 예시와 연결

### 사용자 원문

> 3페이지의 **경로 기반** — 필수 경로가 공식과 달라졌나? 없거나 공식과 똑같으면(커스텀 소실) **차단**.
> **커밋 이력** — 커밋에 `Customization-ID: BANK-OM-001` 꼬리표 + 선언 범위 밖 변경 확인.
> **기능별 필수 동작 검사** — 위 동작 기준을 확인하는 `test_crud_search_roundtrip`을 실제 실행. 일부 기능은 커스텀을 빼면 테스트도 실패하는지 추가 확인했다.
> **검사 대상 고정** — 검사한 커밋과 소스 전체 내용 식별값을 결과에 함께 기록 → 코드가 바뀌면 감지.
> 이 설명들을 왼쪽 관리파일들 예시 들면서 설명할 건데, 자연스럽게 프레젠테이션을 이어갈 수 있도록 설명을 좀 더 이해하기 쉽게 바꿔줄 수 있을까? 간략하게.

### 사용자 추가 요청

> 위 네 가지 외에 저기에 명시되지 않은 검사 규칙도 있다는 점을 추가한다.
> `그 밖에 ~, ~, ~ 검사 등 정형화된 파일을 기준으로 검사를 진행한다`는 의미의 문장을 넣는다.

### 수정 전 문장

1. `경로 기반 — 필수 경로가 공식과 달라졌나? 없거나 공식과 똑같으면(커스텀 소실) 차단.`
2. `커밋 이력 — 커밋에 Customization-ID: BANK-OM-001 꼬리표 + 선언 범위 밖 변경 확인.`
3. `기능별 필수 동작 검사 — 위 동작 기준을 확인하는 test_crud_search_roundtrip을 실제 실행. 일부 기능은 커스텀을 빼면 테스트도 실패하는지 추가 확인했다.`
4. `검사 대상 고정 — 검사한 커밋과 소스 전체 내용 식별값을 결과에 함께 기록 → 코드가 바뀌면 감지.`

### 수정 후 문장

1. `Manifest 경로 확인 — 왼쪽 ①에 적힌 핵심 파일이 후보 코드에 남아 있고 커스텀 내용이 반영됐는지 확인한다. 빠졌거나 공식 코드로 돌아갔다면 차단한다.`
2. `Manifest·커밋 대조 — ①의 기능 ID·허용 경로를 Git 이력과 비교해, 어느 커스텀 기능의 변경인지 표시되지 않은 커밋과 계획 밖 수정을 찾는다.`
3. `Contract 테스트 실행 — 왼쪽 ②에 연결된 test_crud_search_roundtrip을 실행해 생성·조회·수정·삭제·검색 동작이 유지되는지 확인한다.`
4. `검사 결과를 코드에 고정 — 결과에 검사 대상 커밋과 소스 식별값을 함께 기록한다. 이후 코드가 바뀌면 이전 결과를 다시 사용할 수 없다.`

### 추가 설명 문장

- 수정 전: 별도의 종합 설명이 없어 위 네 가지가 전체 검사 규칙처럼 보일 수 있다.
- 수정 후: `그 밖에도 공유 파일 변경, 위험 구역 수정, 필수 경로·테스트 누락 등은 Manifest·Contract·정책 파일에 정해 둔 기준과 실제 결과를 비교해 검사한다.`

## 2. 8페이지 — 검사 결과와 코드 연결을 쉽게 설명

### 사용자 원문

> 그리고 8번에 **결과와 코드 연결**
> 검사한 commit과 코드 내용 식별값을 결과에 기록한다.
> 검사 뒤 코드가 바뀌면 이전 결과를 현재 코드의 근거로 재사용하지 못하게 한다.
> 이것도 약간 엥? 하면서 무슨 소리지 하고 한 번 더 생각하게 돼.

### 수정 전 문장

- 제목: `결과와 코드 연결`
- 본문: `검사한 commit과 코드 내용 식별값을 결과에 기록한다.`
- 보조 문장: `검사 뒤 코드가 바뀌면 이전 결과를 현재 코드의 근거로 재사용하지 못하게 한다.`

### 수정 후 문장

- 제목: `검사한 코드 버전 확인`
- 본문: `검사 결과에는 검사 당시의 코드 버전을 함께 남긴다.`
- 보조 문장: `그 뒤 코드가 바뀌면 이전 결과는 인정하지 않고 새 코드로 다시 검사한다.`

## 3. 10페이지 — 결과 결속 설명을 구체화

### 사용자 원문

> 10페이지에 `최종 commit·테스트 결과·승인을 연결하고, 배포 이미지가 있으면 식별값도 함께 묶는다.`라는 말도, 최신 버전과 커스텀을 적용한 커밋 SHA, 동일한 커밋의 테스트 결과, 동일한 커밋 SHA로 만든 배포 이미지 등 이런 식으로 좀 더 구체화 가능해?

### 수정 전 문장

`D가 최종 commit·테스트 결과·승인을 연결하고, 배포 이미지가 있으면 식별값도 함께 묶는다.`

### 수정 후 문장

`D가 최신 공식 버전에 커스텀을 반영한 최종 commit SHA를 기준으로, 같은 SHA의 테스트 결과와 승인을 연결한다. 배포 이미지가 있으면 같은 SHA로 만든 이미지의 식별값(digest)도 기록한다.`

## 4. 11페이지 — Manifest 후보에서 반영 순서 제거

### 사용자 원문 1

> `그 결과를 목표 경로·추가·삭제·대체·반영 순서가 담긴 새 버전 Manifest 후보로 만든다.`
> 11페이지의 이 문장도 목표 경로가 아니라 `변경 경로의 추가/삭제/대체/반영 순서`라고 해줘. 그리고 반영 순서는 뭐지? 그것도 Manifest에 있었어? 이 질문에 대한 답은 저기에 반영하지 말고 답만 줘.

### 사용자 원문 2

> 그럼 저 설명에서 일단 반영 순서는 빼버리자. 헷갈릴 것 같아.

### 수정 전 문장

`기존 Manifest의 과거 구현 위치를 기준으로 새 공식 버전의 구조를 조사한다. 그 결과를 목표 경로·추가·삭제·대체·반영 순서가 담긴 새 버전 Manifest 후보로 만든다.`

### 수정 후 문장

`기존 Manifest의 과거 구현 위치를 기준으로 새 공식 버전의 구조를 조사한다. 그 결과를 변경 경로의 추가·삭제·대체 계획이 담긴 새 버전 Manifest 후보로 만든다.`

### 같은 기준으로 함께 바꿀 반복 문구

- 수정 전: `기능별 목표 경로·추가·삭제·대체·반영 순서를 담은 Manifest 후보`
- 수정 후: `기능별 변경 경로의 추가·삭제·대체 계획을 담은 Manifest 후보`
- 수정 전: `Manifest 후보의 순서와 목표 구조에 따라 기능을 반영한다.`
- 수정 후: `Manifest 후보에 적힌 변경 계획과 목표 구조에 따라 기능을 반영한다.`

## 5. 12·15페이지 및 반복 설명 — 기능 ID 없는 commit 표현 변경

### 사용자 원문

> 그리고 `기능 ID 없는 commit 0건` 이것도 헷갈리니 워딩을 바꾸든지 지우든지 하는 건 어때? 무조건 좋다고만 하지 말고.

### 판단

- 이 검사는 제품 코드를 바꾼 커밋이 어느 커스텀 기능에 속하는지 추적하기 위해 필요하므로 삭제하지 않는다.
- 다만 `기능 ID 없는 commit`은 구현 용어가 섞여 발표 청중이 바로 이해하기 어려우므로 쉬운 표현으로 바꾼다.

### 15페이지 수정 전 문장

`계획 누락 0건 · 계획 밖 변경 0건 · 기능 ID 없는 commit 0건 · 사람 확인 0건`

### 15페이지 수정 후 문장

`계획 누락 0건 · 계획 밖 변경 0건 · 기능 표시 누락 0건 · 사람 확인 0건`

### 15페이지 보조 설명 수정 전

`Manifest 후보와 최종 Git diff·commit 이력의 일치 여부를 보여준다. 기능 동작 성공은 C에서 별도로 확인한다.`

### 15페이지 보조 설명 수정 후

`제품 코드를 바꾼 각 커밋에 어느 커스텀 기능의 변경인지 표시됐는지와 Manifest 후보·최종 Git 변경의 일치 여부를 보여준다. 기능 동작 성공은 C에서 별도로 확인한다.`

### 다른 페이지의 반복 표현

- 수정 전: `기능 ID 없는 commit`
- 수정 후: `기능 표시가 빠진 코드 변경`
- 수정 전: `기능 ID가 빠진 커밋`
- 수정 후: `어느 커스텀 기능의 변경인지 표시되지 않은 커밋`

## 최종 확인 검색어

최신 덱에 적용한 뒤 다음 문구가 남아 있지 않은지 전체 검색한다.

- `반영 순서`
- `Manifest 후보의 순서`
- `기능 ID 없는 commit`
- `기능 ID가 빠진 커밋`

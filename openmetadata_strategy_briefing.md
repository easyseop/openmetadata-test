# OpenMetadata 커스터마이징 관리 전략 — 도입 배경과 설명 자료

> 이 문서는 상세 설계서(`openmetadata_upstream_customization_design.md`)의
> 내용을 처음 접하는 사람을 위해 풀어 쓴 설명 자료다.
> "왜 이 전략을 쓰는가, 어떻게 도입하는가, 단점은 무엇이고 어떻게 보완하는가"에
> 답하는 것을 목적으로 한다.

---

## 1. 한 장 요약

| 질문 | 답 |
|---|---|
| 무엇을 하려는가 | 오픈소스 OpenMetadata를 행내에 도입하되, 행내 수정(커스터마이징)을 최소한으로 유지한다 |
| 궁극적 목표 | 오픈소스 새 버전이 나와도 **행내 수정을 즉시, 빠짐없이 재적용**하여 버전 업그레이드 운영을 원활하게 만든다 |
| 선택한 전략 | **패치 스택 전략** — 행내 수정을 "이식 가능한 조각(커밋)"으로 유지하고, 새 버전 위에 매번 다시 얹는다 |
| 왜 이 전략인가 | "우리가 뭘 바꿨는지"가 항상 명확하고, 수정 누락을 **기계(CI)가 검출**할 수 있는 유일한 구조이기 때문 |
| 대표 단점 | 업그레이드마다 재적용 작업이 반복된다 |
| 보완책 | 재적용을 스크립트·자동 게이트로 자동화하고, 커스터마이징 수 자체를 정책으로 억제한다 |

---

## 2. 배경: 무엇이 문제인가

우리는 오픈소스 OpenMetadata를 외부망에서 개발·검증한 뒤 내부망에 반입하여
운영한다. 이때 행내 요구사항(SSO 연계, 보안 헤더, UI 정책 등) 때문에
오픈소스 코드 일부를 수정해야 한다.

문제는 **오픈소스는 계속 새 버전이 나온다**는 것이다.

```text
공식 1.5 + 행내 수정 3건  ← 지금 운영 중
공식 1.6 발표             ← 보안 패치·신기능 포함. 언젠가 올라가야 함
```

1.6으로 올라갈 때 행내 수정 3건을 다시 반영해야 하는데, 여기서
전형적으로 발생하는 사고가 다음과 같다.

1. **수정 누락** — 3건 중 1건이 조용히 사라져도 아무도 모른다.
   빌드는 되고 화면도 뜨는데, 몇 주 뒤 "SSO가 왜 안 되죠?"로 발견된다.
2. **이유 소실** — "이 코드는 왜 고쳐져 있지?" 담당자가 바뀌면
   수정의 목적을 아무도 모르게 된다.
3. **업그레이드 공포** — 위 두 가지가 무서워서 버전을 안 올리게 되고,
   보안 패치가 밀리며 오픈소스와의 격차가 계속 벌어진다.

이 문제를 구조적으로 막는 것이 이번 전략의 목적이다.

---

## 3. 두 가지 전략: 무엇을 비교했는가

행내 수정을 유지하면서 오픈소스 버전을 올리는 방법은 크게 두 가지다.

### 방식 A. 벤더 브랜치 전략 (merge 방식) — 전통적 방식

행내 수정 브랜치를 영구히 하나 두고, 새 버전이 나오면
그 브랜치 안으로 **부어 넣는다(merge)**.

```text
vendor(공식):   1.5 ──────── 1.6
                  \            \
custom(행내):      수정3건 ──── [merge] ──→ 계속 사용
```

### 방식 B. 패치 스택 전략 (재적용 방식) — 이번에 선택한 방식

행내 수정을 **독립된 커밋 3개**로 유지하고, 새 버전이 나오면
순정 새 버전 위에 그 3개를 **다시 얹는다(cherry-pick)**.

```text
vendor(공식):   1.5              1.6
                  \                \
custom(행내):      수정1·2·3        수정1'·2'·3'   ← 새로 만든 브랜치
                 (custom-1.5)      (custom-1.6)
```

### 비유로 이해하기

- **방식 A**는 원본 책에 내 메모를 섞어 쓰고, 개정판이 나오면
  두 책을 **한 권으로 합치는** 것이다. 몇 번 합치고 나면
  어디가 원본이고 어디가 내 메모인지 알 수 없게 된다.
- **방식 B**는 내 메모를 **포스트잇 3장**으로 유지하는 것이다.
  개정판이 나오면 포스트잇을 떼서 새 책에 다시 붙인다.
  붙이는 수고는 매번 들지만, 내 메모가 무엇인지 언제나 명확하고,
  **한 장을 빠뜨리면 즉시 티가 난다.**

---

## 4. 왜 방식 B(패치 스택)를 선택했는가

핵심 판단 기준은 두 가지 질문이었다.

### 질문 1. "우리가 오픈소스에서 바꾼 게 정확히 뭐예요?"

- **방식 A**: merge를 몇 번 거치면 행내 수정이 merge 기록 속에 흩어져
  이 질문에 답할 수 없게 된다. 감사·보안 검토·담당자 인수인계 때마다
  문제가 된다.
- **방식 B**: `공식 태그 이후의 커밋 목록`이 곧 답이다.
  3년 뒤에도 명령어 한 줄로 수정 3건이 그대로 나온다.

### 질문 2. "수정 누락을 기계가 잡아낼 수 있는가?"

- **방식 A**: 불가능하다. merge 결과물은 겉보기에 멀쩡하므로
  수정이 절반쯤 사라져도 검출 수단이 없다. 사람의 기억에 의존한다.
- **방식 B**: 가능하다. "명세에 등록된 수정 ID 3개 = 브랜치의 커밋 3개"라는
  **집합 비교**를 CI가 수행하고, 하나라도 다르면 빌드를 실패시킨다.
  누락이 사람의 실수가 아니라 **CI 실패라는 사건**으로 드러난다.

우리의 궁극적 목표가 "버전이 올라가도 즉시·빠짐없이 재적용"이므로,
"빠짐없이"를 기계로 보장할 수 있는 방식 B가 목표에 부합한다.

참고로 이 방식은 특이한 선택이 아니다. Debian·Fedora의 패키지 패치 관리,
Android 벤더 포크 등 오픈소스 위에 자체 수정을 유지하는 조직들의
표준적인 방식이다.

---

## 5. 이 전략의 단점과 보완책

정직하게 말하면 방식 B에도 비용이 있다. 각 단점에 대해
보완책이 "어떻게 동작하는지"까지 구체적으로 기술한다.

### 단점 1. 업그레이드마다 재적용 작업이 반복된다 → 보완: 자동화

어떻게 동작하는가:

1. 적용 순서를 `patch-order.txt`에 고정해 두고 스크립트가 순서대로
   자동 적용한다. 수정이 3건이든 10건이든 **명령 1회 실행**으로 끝난다.
2. Git은 수정을 줄 번호가 아니라 **주변 코드의 문맥**으로 이식한다.
   새 버전에서 코드 위치가 밀리거나 근처가 바뀌어도 스스로 자리를 찾는다.
3. 버전마다 같은 자리에서 나는 반복 충돌은 Git이 이전 해결책을
   기억해 두었다가(`git rerere`) 자동으로 재생한다.

예시: 공식이 건드리지 않은 수정 2건은 개입 0으로 자동 통과하고,
사람은 충돌 난 1건의 해당 지점만 해결한다.

### 단점 2. 공식이 우리 수정 부분을 바꾸면 충돌이 난다 → 보완: 격리 + 기록

어떻게 동작하는가:

1. 충돌 자체는 어떤 전략을 써도 피할 수 없다(두 주체가 같은 코드를
   고친 것). 관건은 **어디서 터지느냐**다.
2. 커밋 1개 = 수정 1건이므로 충돌이 나면 **"어느 ID를 얹다가 어느
   파일에서 났는지"가 즉시 특정**된다. 담당 팀이 그 지점만 보면 된다.
   merge 방식은 거대한 병합 안에서 한꺼번에 터져 원인 분리가 안 된다.
3. 해결 내용은 충돌 기록 문서에 의무 기록한다: 무엇 때문에 충돌했고,
   행내 로직을 유지했는지 공식 기능으로 대체했는지.

예시: "BANK-OM-001 적용 중 AuthenticationFilter.java 충돌"
→ SSO 담당 팀이 해당 파일의 해당 지점만 확인·해결.

### 단점 3. 수정이 수십 건으로 불어나면 비용이 커진다 → 보완: 원천 억제

어떻게 동작하는가:

1. 새 요구사항은 **4단계 관문**을 순서대로 통과해야 코어 수정이
   허용된다: ①공식 설정으로 가능? → ②배포 설정(Helm)으로 가능? →
   ③코어 밖 확장 모듈로 가능? → ④전부 불가할 때만 코어 수정 승인 절차.
2. 코어 수정 승인에는 ID·의사결정 기록(ADR)·자동 테스트·제거 조건이
   필수다. 없으면 CI가 커밋 자체를 거부한다.
3. 패치 개수·변경 라인 수에 상한을 두고 Gate 4가 상시 감시하며,
   초과 시 재설계 검토 없이는 릴리스가 불가하다.

예시: "DB2 메타데이터 수집" 요구 → 코어 수정 대신 ③단계의
별도 커넥터 패키지로 구현 → 패치 0건 증가.

### 단점 4. 매 버전 브랜치를 새로 만들어 이력이 재작성된다 → 보완: 정본 규칙

어떻게 동작하는가:

1. "현재 유효한 수정이 어디 있는가"를 규칙으로 고정한다:
   재적용 소스는 **항상 직전 릴리스 브랜치의 최신 리비전**.
2. 운영 중 수정에 버그가 나면 현재 릴리스 브랜치에서 고치고
   리비전을 올린다 (`1.5-bank.1` → `1.5-bank.2`).
3. 다음 버전(1.6) 작업은 반드시 최신 리비전(`bank.2`)에서 가져오므로
   핫픽스가 자동 전파되고, 이전/신규 패치 묶음의 차이는
   비교 리포트(`git range-diff`)로 검증한다.

예시: 1.5-bank.2에서 고친 SSO 버그가 1.6-bank.1에 자동 포함되며,
유실되면 비교 리포트에서 검출된다.

### 단점 5. 자동 충돌 해결이 틀린 결과를 재생할 수 있다 → 보완: 검출 강제

어떻게 동작하는가:

1. 자동 해결(rerere)은 "이전에 이렇게 풀었다"의 기계적 재생일 뿐,
   **그 해결이 여전히 옳은지는 보장하지 않는다.** 이것이 위험 지점이다.
2. 따라서 CI가 **자동 해결이 개입한 커밋을 검출**해 목록화한다.
   개입 사실 자체가 기록에 남는다.
3. 검출된 수정은 필수 테스트 전체 실행 + 코드 리뷰 승인이 강제되며,
   통과 전에는 릴리스 승격이 차단된다.

예시: SSO 충돌이 자동 해결됨 → SSO 테스트 4종 전체 실행 +
담당 팀 리뷰 승인 후에만 릴리스 진행.

보완책 1·3·5는 사람의 주의력이 아니라 **자동 게이트**로 구현한다(7절 참조).

---

## 6. 실제로 어떻게 동작하는가 — 1.5 → 1.6 업그레이드 예시

행내 수정 3건이 있다고 하자.

```text
BANK-OM-001  행내 SSO 인증 연계
BANK-OM-002  필수 보안 헤더 적용
BANK-OM-003  행내 UI 정책 적용
```

각 수정에는 고유 ID가 있고, ID마다 명세 파일(YAML)이 있어
목적·담당 팀·영향 파일·필수 테스트·제거 조건이 기록되어 있다.

### 단계 1. 새 버전 접수와 영향 예측 (자동)

1.6이 발표되면 CI가 1.5와 1.6의 변경 파일 목록을 뽑아
각 수정의 "영향 파일" 목록과 대조한다.

```text
1.6에서 인증 코드 변경됨 → BANK-OM-001 영향 가능, SSO 테스트 필수
1.6에서 UI 로그인 화면 변경됨 → BANK-OM-003 영향 가능, UI 테스트 필수
BANK-OM-002는 무관 예상
```

### 단계 2. 재적용 (자동 + 충돌 시에만 사람)

순정 1.6에서 새 브랜치를 만들고 스크립트가 수정 3건을 순서대로 얹는다.

```text
BANK-OM-002  → 자동 적용 성공 (사람 개입 0)
BANK-OM-001  → 충돌 발생 → 담당자가 해당 지점만 해결
BANK-OM-003  → 충돌 발생 → 1.6의 새 UI 구조에 맞게 수정
```

### 단계 3. 완전성 검증 (자동)

CI가 검사한다: "명세의 활성 ID 3개가 새 브랜치의 커밋 3개와
정확히 일치하는가?" 하나라도 빠지면 **빌드 실패**. 누락은 여기서
반드시 걸린다.

### 단계 4. 테스트 (자동)

영향받은 수정의 필수 테스트(SSO 로그인, UI 회귀 등)와
DB 마이그레이션·검색·수집 파이프라인 검증을 자동 수행한다.

### 단계 5. 릴리스

전 단계를 통과하면 `1.6-bank.1`이라는 행내 버전이 만들어진다.
어떤 공식 버전 + 어떤 수정 3건 + 어떤 테스트 결과로 구성되었는지가
릴리스 잠금 파일에 기록되어 언제든 재현·감사할 수 있다.

**전체에서 사람 손이 간 곳은 충돌 2건의 해결뿐이다.**
나머지는 전부 자동이며, 수정이 적고 격리되어 있을수록
사람 개입은 0에 수렴한다.

---

## 7. 판단을 건너뛰지 못하게 하는 자동 게이트

이 전략의 신뢰성은 4개의 자동 게이트가 담보한다.
모두 성공/실패가 기계적으로 판정되며, LLM이나 사람의 감에 의존하지 않는다.

```text
Gate 1  재적용 게이트    모든 수정이 새 버전에 적용되었는가
                        (하나라도 실패하면 어느 ID인지 보고하고 중단)

Gate 2  완전성 게이트    명세의 ID 목록 = 브랜치의 커밋 목록인가
                        (누락·미등록을 집합 비교로 검출)

Gate 3  자동해결 검출    자동 재생된 충돌 해결이 있으면
                        테스트·리뷰 없이 릴리스 승격 금지

Gate 4  부채 상한 게이트  코어 수정 개수·2회 연속 충돌 패치가
                        상한을 넘으면 재설계 검토 없이는 실패
```

사람의 판단이 필요한 곳(충돌 해결, 리뷰)은 남기되,
게이트는 그 판단을 **생략하지 못하게** 막는 역할을 한다.

---

## 8. LLM 위키를 도입한다면 — 구체적 사용 방식

LLM 위키는 판정 장치가 아니라 **탐색·설명 계층**이다.
진실의 원본(Git·명세 YAML·테스트 결과·ADR)에서 자동 생성되며,
7절의 게이트 체인에는 개입하지 않는다.

### 생성 흐름

```text
진실의 원본                          LLM 위키                자연어 질의
Git 이력 · 명세 YAML      →  CI가    →  수정·요구사항·      →  담당자가 말로 묻고
테스트 결과 · ADR            자동 생성   테스트·장애 이력이      근거 링크가 달린
                                       연결된 관계 페이지       답을 받음
```

동기화 규칙: 위키 내용이 원본과 다르면 항상 원본 기준으로 재생성한다.
**위키는 원본의 캐시이지 제2의 원본이 아니다.**

### 위키 페이지 하나의 구조

수정 1건을 중심으로 모든 관계가 연결된다.

```text
[[BANK-OM-001 행내 SSO 인증 연계]]
  ├─ [[REQ-AUTH-017]]         왜 필요한가 (업무 요구사항)
  ├─ [[ADR-001]]              왜 이 방식인가 (검토한 대안과 결정 근거)
  ├─ [[Authentication 모듈]]   어디를 고쳤는가 (영향 파일·설정 키)
  ├─ [[TEST-SSO-001~004]]     무엇으로 검증하는가 (필수 테스트)
  ├─ [[1.6 업그레이드 리포트]]  버전별로 어떻게 변해왔는가
  └─ [[인증 장애 대응 이력]]    과거에 어떤 문제가 있었는가
```

### 실제 사용 시나리오 4가지

1. **신규 담당자 인수인계** — "SSO 수정은 왜 있는 거예요? 지워도 되나요?"
   → 요구사항·ADR·제거 조건을 근거 링크와 함께 설명. 전임자가 없어도
   수정의 목적과 "언제 지울 수 있는지"까지 파악 가능.
2. **새 버전 영향 질의** — "1.7에서 인증 모듈이 바뀌면 뭐가 영향받죠?"
   → 경로 분석 결과에 의미적 해석을 더해 영향 후보 수정·필수 테스트
   목록을 제시. 단, "영향 없음 확정"은 말하지 않는다 — 확정은 테스트가 한다.
3. **충돌 배경 조사** — "공식이 이 코드를 왜 바꿨지?"
   → 공식 릴리스 노트·변경 이력을 요약해 충돌의 배경을 설명.
   "우리 로직 유지 vs 공식 기능 대체" 판단 시간을 단축.
4. **운영 장애 원인 탐색** — "로그인 장애인데 관련된 행내 수정이 있나?"
   → 장애 증상과 관련된 수정·설정 키·과거 장애 문서를 연결해
   원인 후보를 좁힘. 새벽 장애 때 "이 코드 누가 알지?"를 없앤다.

### 하는 일과 하지 않는 일 — 경계가 신뢰의 조건

| 위키·LLM이 하는 일 | 하지 않는 일 (게이트가 담당) |
|---|---|
| 변경 요약, 관계 탐색, 영향 후보 제안, 테스트 후보 제안, 문서 초안 — 모두 근거 링크를 달아 제시 | 배포 승인, "누락 없음" 최종 판정, 테스트 생략 결정, 마이그레이션 성공 판정 |

이 경계 덕분에 **LLM이 틀려도 배포 판정에는 영향이 없다.**

### 도입 시점

로드맵의 마지막 단계(Phase 6)다. 수정·문서가 늘어나 여러 저장소를
가로지르는 탐색이 필요해질 때 도입하며, 그 전에 원본(ID·명세·테스트)
체계가 먼저 서 있어야 위키가 신뢰할 수 있는 데이터 위에서 생성된다.

---

## 9. 도입 로드맵

한 번에 도입하지 않고 6단계로 나누며, 각 단계가 독립적인 가치를 갖는다.

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1. 현황 조사 | 현재 수정된 파일 전체를 추출하고 유형 분류 | 목적 불명 수정이 식별됨 |
| 2. ID·명세 도입 | 모든 수정에 `BANK-OM-xxx` ID와 명세 YAML 부여 | 모든 수정에 담당 팀·테스트 존재 |
| 3. 패치 스택·CI | 버전별 브랜치 체계와 게이트 구축 | 수정 누락 시 CI가 실패함 |
| 4. 업그레이드 자동 테스트 | DB·검색·수집·인증 검증 자동화 | 후보 버전 전체 테스트 반복 가능 |
| 5. 반입 패키지 | 내부망 반입·재검증 체계 (후속 단계) | 외부망·내부망 산출물 동일성 검증 |
| 6. LLM 보조 | 영향 분석·문서화 보조 (선택) | LLM이 CI 결과를 대신하지 않음 |

**1차 목표는 1~4단계**(업그레이드 재적용 체계)이며,
내부망 반입(5단계)은 그 이후의 후속 과제다.

---

## 10. 기대 효과

| 항목 | 도입 전 | 도입 후 |
|---|---|---|
| 수정 누락 | 사람 기억에 의존, 운영에서 발견 | CI 실패로 사전 검출 |
| "뭘 바꿨나" 질문 | 답변 불가능해짐 | 명령어 한 줄로 즉답 |
| 업그레이드 소요 | 예측 불가, 회피 경향 | 반복 가능한 절차, 대부분 자동 |
| 담당자 교체 | 지식 유실 | ID·명세·ADR로 목적 보존 |
| 보안 패치 대응 | 격차 누적 | 신속한 버전 추종 가능 |

성공 여부는 감이 아니라 지표로 측정한다: 활성 코어 수정 수(감소),
자동 적용 비율(증가), 누락 검출 건수, 업그레이드 소요 시간(감소) 등.

---

## 11. 맺음말

이 전략의 본질은 도구가 아니라 **원칙**이다.

1. 오픈소스 원본은 건드리지 않고, 행내 수정은 최소한의 독립 조각으로 유지한다.
2. 모든 수정에 지워지지 않는 ID를 붙여 목적·담당·테스트를 추적한다.
3. "빠짐없이 적용되었는가"를 사람이 아니라 CI가 판정하게 한다.

이 세 가지가 지켜지는 한, 오픈소스가 아무리 빨리 발전해도
행내 버전은 통제된 비용으로 따라갈 수 있다. 상세 구현은
설계서(`openmetadata_upstream_customization_design.md`)를 참조한다.
